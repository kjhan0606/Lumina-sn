# 재개 노트 — 2026-08-08 (compact 직전)

문맥 압축 시점의 **살아 있는 상태**. 새 문맥에서 이것부터 읽는다.

---

## 1. 지금 돌고 있는 것 (compact 후에도 계속 돈다)

| PID | 무엇 | 산출 | 다음 |
|---|---|---|---|
| 3812718 | **Codex R7** (gpt-5.6-sol, effort high) — 발행 위상 수리 발주서+코드 | `/tmp/claude-10396/codex_r7/OUT_R7.md` | 착지 시 아래 체인이 자동 검사 |
| 3823381 | `scripts/r7_apply_and_check.sh` — R7 정적 적신호 검사 | `/tmp/claude-10396/r7_check.log` | 적신호 없으면 백업 후 **적용 대기**(수동) |
| 3813578 | `chain_r7.sh` — 구조 확인 | `/tmp/claude-10396/r7_chain.log` | |

★적신호가 켜지면 **적용하지 않고 멈춘다**(새 env 노브·clamp/floor·세대 술어 변경).
계약의 **의미론** 완화는 기계가 못 본다 — 운전석이 읽는다.

또 진행 중이었던 것: 회색 moment 솔버 발주서(`/tmp/claude-10396/codex_grey/ORDER_GREY.md`),
Codex 정합성 검토(`/tmp/claude-10396/codex_consist/OUT_CONSIST.md`, 이미 착지·반영됨).

---

## 2. 오늘 확정된 것

### 아침 — HEAD 가 런을 시작조차 못 했다
K-FRESH(a97d0e1, 08-05) ↔ A2-07(3ddd95c, 08-06) **순서 충돌**.
「고리 밖 감사 층 0: 계약 10건 폐합」 커밋이 **런을 닫았다**.
정본 `docs/BLOCKER_KFRESH_TE_GENERATION.md`.

### 낮 — 재배선 파이프라인 D→E→F→G 완주
- Codex D(coevolution vs two-arm) → Fable E(인증) → Codex F(함수목록+배선도) → Fable G(**APPROVED WITH CONDITIONS**, 조건 8건)
- ★Fable E 가 운전석 전제를 **`OVERSTATED`** 로 판정: "두 팔의 일치는 **차동 잣대**일 뿐
  공통모드(원자자료·공유 source closure·공유 population·동일 1000-bin 격자·BC·계약·덱 잔재)에 **맹목**".
  논문 자신이 3% 일치 + CMFGEN 대비 1.2–1.8 dex 오류 사례를 싣고 있다(`paper_main.tex:867-876`).
- 정본 `docs/LAYER1_REPLAN_2026-08-07.md`(L1-1~L1-6, +L1-5b 비열적 퇴적),
  근거 `validation/layer1_replan/OUT_{A,B,C,D,E,F,G}*.md`.

### 저녁 — L1-1 착지, K-FRESH 처음 통과
벽 4개(T_e 세대 0 · 최상단 이온 거부 · n_e 미수렴 · POP_BF_STALE) 해소.
부트스트랩 창 = **런당 1회 래치**, env 로 못 연다.  판정 `validation/l1_1_gates/L1_1_GATE_VERDICT.md`.
- PASS: G4(발행 9항목) · G5(창 8항목) · G6(덱 3종) · R0 음성대조(재실행)
- 미달: G1 → **R7 소관**(기대 결과) · G7 → 문턱 미정(물리 결정)

### 저녁 — 32이온 격리 덱을 구웠다
`data/tardis_reference_toy06_19p48d_sivcaiv_active` — 활성 27이온/6원소(Z=14,16,20,26,27,28).
몇 달 전 설계됐으나 **굽지 않았던** 것(문서가 "prepares, but does not run" 이라 적어 둠).
V I 이 준위 1개짜리 자리표시자라 V II 가 최상단이 되던 문제가 이걸로 해소.
**user 결정: 승격**(정본으로 삼는다).

### 밤 — catalog 자료가 절반 깨져 있었다
3,480행 중 1,636행이 `g=0` — osc **전이 블록**을 준위로 읽어 `f`→`g`, `A`→`E`.
3단계로 정화: 블록 경계 → 준위 누락(문자없는 이름·음수 ID) → **vintage 혼입**(S VI 만 1999 년판).
현재 15이온 **7,242준위, 전 이온 19apr23 단일 vintage**, 검사 위반 0.

---

## 3. ★업무분장 (개정10, 2026-08-07 저녁)

| 일 | 담당 |
|---|---|
| 발주서 저작 · **가설 수립** · **코딩** | **Codex** |
| **코드 검사** · 실행 · 제출 · 판정 · 대장 | **운전석** |
| 물리 판정·인증·최종 승인 | **Fable** |

근거: 운전석 코딩 실패 3건(`sscanf` UB · 검증된 판독기 두고 새로 짬 · `grep -q`+pipefail).
정본 memory `feedback_order_written_by_driver_audited_by_codex.md`.

★Codex 호출 시 반드시: **cwd=/tmp 스테이징**(저장소 주면 죽는다) · 증거 인라인 ·
**이미 있는 자산 명시**(새로 짜지 말라고) · 개방 탐색 금지.

---

## 4. 검사 자산 (오늘 만든 것 — 전부 양방향 대조 통과)

| 검사기 | 무엇을 보는가 | 대조 |
|---|---|---|
| `scripts/check_topion_catalog.py` | C1~C8: g 양의 정수 · 바닥 E=0 · **Z(T)≥g₀(수학적 하한)** · 등전자 · **vintage 일관성** | 깨진 자료 FAIL / 정상 PASS |
| `scripts/check_publication_phase.py` | P1~P5: a209 발행 · 동세대 · **a208 결박** · blocked_stale · (P5=R6 분리) | 수리 전 로그 FAIL / 정상 PASS |
| `tests/seed_te_publish_selftest.c` | seed 발행 fail-closed 9항목 | PASS |
| `tests/bootstrap_window_selftest.c` | 창 재진입·래치 8항목 | PASS |
| `scripts/check_makefile_headers.py` | 헤더 의존성 21/21 | PASS |
| `scripts/check_legacy_knob_registry.py` · `verify_seals.py` | 노브 레지스트리 · 봉인 | PASS |

★`check_publication_phase.py` 음성대조가 **a208 도 현 복사장에 결박 안 됨**(`rad=0` vs `gen=1`)을
새로 적발했다 — a209 부재만 문제가 아니다.

---

## 5. 다음 (순서)

1. **R7 착지 → 검사** — 정적 적신호 → 적용 → 빌드 → `check_publication_phase.py` →
   음성대조(a209 를 T_e 뒤로 되돌리면 `blocked_stale` 발화하는가) → 덱 3종
   ⚠R7 만으로 `line: status=-1 gen=0` 은 안 풀린다 — **R6 소관, 기대 결과로 사전등록됨**
2. R6(결정론 line-J̄) → R8(세대 보존) → R5(barrier) → R10(3열 지도)
3. 물질 사슬(R0~R4) 완성 후 **1차 Codex H**(구현·배선 검사), 두 팔 합류 후 2차
4. 운전석 I(검증 계획) — ★새로 짜지 말고 **기존 규약 상속**:
   `docs/CODEX_CMFGEN_EQUIVALENCE_PLAN_v2.md`(M/E/S 동등 3분류 + 정량 문턱 + 판정 계산 규약) ·
   `scripts/oracle_compare_cmfgen.py`(RVTJ·PRRR·ion OUT·GENCOOL fail-closed 파서)

---

## 6. user 판단 대기

| # | 항목 | 상태 |
|---|---|---|
| 1 | **R1 물리적 seed T_e 단일 선언식** | 평면 Eddington **실측 기각**(median 37.4% vs 현행 22.9%; τ_es 총 5.41 로 두껍지 않음). CMFGEN 은 회색 **moment** 를 푼다(`comp_grey_v4.f:277` `TGREY=(πJ/σ)^¼`). 회색 moment 발주서 Codex 작업 중. Fable 은 식을 추천하지 않고 **제약만**: 기하·BC 만 · CMFGEN 답/`plasma_state.csv` 주입 금지 · **파라미터화되는 순간 노브가 된다** |
| 2 | **n_e 수렴 문턱** (Fable G 조건 8) | 실측 잔차 **1.586e-02**, 덱 3종에서 **동일** ⟹ 조성이 아니라 **구조적**. G7 재설계에 필요 |
| 3 | catalog 가 `population_atomic_model_sha256` 에 **안 들어감** | catalog 바꿔도 stamp 동일 → GEN-GUARD 침묵. A2-07 관점 판정 필요 |

받은 결정: 격리 덱 **승격** · A2-13 **사후검증** · 챔피언 **새 기준 수립**.

---

## 7. 오늘 반복된 실패 형태 (자기 기재)

- **내가 만든 계측이 내 런을 막은 것 3회**: `ENV_STRICT` 전집 정의 결함 · 노브 레지스트리
  4번째 강제 사이트 누락 · `grep -q`+pipefail
- **감시가 거짓말한 것 3회**: slurm `CANCELLED+` 접미사 미매칭 · 변화 없이 과다 발화 ·
  `pgrep -f` **자기매칭**(3번 걸렸다 — 확실한 것은 PID 기반 `kill -0` 또는 argv 직접 확인)
- **음성대조가 무효였던 것 2회**: 주입이 no-op(잘못된 stage 지목) · 주입은 됐으나 별도 결함이 덮음
- **내 가설이 틀린 것 6회 이상** — 답이 나온 것은 전부 **침묵하는 실패 경로에 진단을 붙여
  사실을 받은 경우**였다

★규약으로 삼은 것: **주입한 결함이 실재하는지 독립 검사로 먼저 확인하지 않으면
음성대조는 무효다.** "주입했는데 통과" 를 "결함 없음" 으로 읽으면 안 된다.

# Wave-3.2 R7 authoritative frozen-field capture contract.
# Producer selection only.  The consumer contract is enforced by
# scripts/cmf_chieta_check.py (expected iteration defaults to 10).
export LUMINA_CMF_FROZEN_CHIETA_ITER=10

# ★잣대 감사 — CMFGEN 기준런이 수렴하지 않았고, 그 산출로 11일을 대조했다

2026-08-19, 전략 감사(Fable 독립 컨텍스트)가 지목 → 운전석 실측 확인.
프로젝트 상설 규약 「잣대부터 감사」의 사례 14.

## 실측

`/gpfs/kjhan/cmfgen_runs/toy06_19p48d_ophys/OUTGEN` (mtime **2026-08-06 20:06**, 13일 무손):
- `Maximm changes as returned by SOLVEBA_V13` 기록 **63회**
- 마지막 12회: `86954 · 1e7 · 1e7 · 58217 · 1e7 · 117736 · 616614 · 30506 · 1e7 · 1e7 · 1e7 · 1e7`
- ⟹ **진동·발산**. 목표는 ≤1%. 수렴·정상종료 표지 없음.

그리고 **우리가 대조에 쓴 파일이 같은 런·같은 시각의 산출이다**:
`LINEHEAT`·`NETRATE`·`OUTGEN` 전부 mtime `08-06 20:06`.
`cmfgen_coordinate_reference.json` 이 그 둘을 SHA 로 봉인해 참조한다
(`lineheat_sha256=e533a503…`, `netrate_sha256=3066c9e9…`).

## 처분 — 08-17 결론의 이분 분리

| 결론 | 오라클 의존 | 처분 |
|---|---|---|
| `lumina_cancellation_condition = 1.0` (정확히), `absolute_signed_sum == signed_rate` 17자리 동일 ⟹ **1,282선 중 순-흡수 선 0개** | **없음** — 순수 Lumina 내부 성질 | **유효 유지**. 이것이 현재 가장 강한 발견이다 |
| CMFGEN 이 양 부호를 모두 가진다(상쇄조건 27.5–105.2) | 약함 — 미수렴이어도 상쇄조건 ≫1 은 양 부호 존재를 함의 | **정성만 유지**, 수치 인용 금지 |
| 흡수 1/172, net 비 4.37e5(또는 5.75e5), 방출 1.41× | **강함** — 분모가 미수렴 상태 | **보류**. 재인용 금지 |
| `validation/gate_b_dual_oracle/parity59/oracle_vs_cmfgen.md` 커버리지 17.01% | 강함 | **보류** |

⟹ **A2-10 의 "오라클 없음" 은 선택이 아니라 사실이며, 소급된다.**
`OPHYS_RESUME_NOTE.txt`(08-06)가 "STAGE-1 산출물은 다른 게이트의 ORACLE_INPUT 으로 유효" 라
적었으나, 그 STAGE-1 자신이 수렴하지 않았다. 그 선언의 전제가 무너진다.

## 임계경로에 미치는 영향 — **없다**

현재 표적(선 흡수항 결손)의 근거는 **오라클 무의존 발견**이다:
1,282선 전부가 같은 부호라는 것은 CMFGEN 없이도 결함이다.
실제 복사장에서 모든 선이 순-방출일 수 없다.
⟹ 흡수항 작업은 잣대 붕괴와 무관하게 진행한다.

## 남는 일

- CMFGEN STAGE-1 재개 또는 발산 원인 진단(별도 단). grammar slurm, **OMP=16 절대**.
- 그때까지 모든 "CMFGEN 대비 N배" 진술에 `[오라클 미수렴]` 표지를 단다.

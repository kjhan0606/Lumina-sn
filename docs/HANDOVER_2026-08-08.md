# LUMINA-SN 업무 인수인계서 — 2026-08-08

인계 시점 상태를 **실측으로** 적는다.  기억으로 쓴 항목은 없다.
각 절의 근거 문서를 함께 적었으니, 의심되면 그 문서를 먼저 읽는다.

---

# 0. 프로젝트가 무엇인가

**Ia 초신성 스펙트럼을 CMFGEN 수준으로 재현하는 복사수송 코드**를 만든다.

| | |
|---|---|
| 규모 | `src/` 55 파일 · **55,719 행**(C/CUDA).  최대 파일 `lumina_plasma.c` 17,739 행 |
| 덱 | `data/tardis_reference_*` **94종**.  기준 = toy06 @ 19.48 d |
| 커밋 | 총 300 (2026-08-08 하루에 22) |
| 문서 | `docs/` **385 md** — 대부분 과거 캠페인 원장.  현행은 §4 참조 |

## 0.1 두 팔 구조 (이 프로젝트의 고유 설계)

논문 Fig.1 의 핵심.  **같은 플라즈마 상태 위에서 두 가지 복사수송을 돌린다.**

```
                    ┌─ MC 팔      : Monte Carlo 패킷 수송 → 표본 추정자 J_ν
공유 물질 상태 ─────┤
                    └─ 결정론 팔  : CMFGEN 방식 formal solve → 무잡음 J_ν
```

★**두 팔의 일치는 "독립 잣대" 가 아니다** — Fable 판정(2026-08-07):
공통모드(원자자료·공유 source closure·공유 population·동일 격자·BC·계약·덱 잔재)에
**맹목**인 **차동 잣대**다.  논문 자신이 두 장이 3% 안에서 일치한 채 CMFGEN 대비
UV 광이온이 1.2–1.8 dex 틀린 사례를 싣고 있다(`paper_main.tex:867-876`).

## 0.2 지금의 목표 (2026-08-08 user 확정)

> **"지금은 CMFGEN 을 제대로 구현하는 것이 목표이고, CMFGEN 의 단점을 그대로
> 계승하는 것은 나쁜 것이 아니다.  CMFGEN 의 발자국을 그대로 가는 것이 중요."**

- CMFGEN 에 대응물이 **있는** 것(결정론 팔·공유 기계): 알고리즘·근사·순서를 **그대로** 옮긴다.
  더 정확한 대안이 있어도 지금은 채택하지 않고 **조용히 대장에 기재**한다
- **없는** 것(MC 전용 — 사건 측도·패킷·추정자): Lumina 자신의 계약이 기준
- 잣대는 **동종 대 동종**(CMFGEN 의 같은 루틴 출력)
- 재현 전 개량은 **차이의 귀속을 불가능하게** 만든다

정본: memory `feedback_follow_cmfgen_footsteps_not_improve`

---

# 1. 캠페인 구조 — 「고리 밖 감사」

개시 2026-08-03.  정본 memory `project_outside_the_loop_audit.md`,
증거 풀 `docs/OUTSIDE_LOOP_POOL.md`(2,108행).

**문제의식**: 닫힌 고리에서 역추적은 원리적으로 순환한다(자기되먹임이 감도를 은폐).
⟹ **고리가 소비하되 생산하지 않는 것**을 감사한다.

**우선순위 = 중요도가 아니라 독립도**(위상정렬).  미검증 의존이 0 인 것부터 닫는다.

| 층 | 뜻 | 상태 |
|---|---|---|
| **0층** | 계약 자산.  음성대조로 FAIL 을 시연한 뒤 얻은 것 | 10건 확보(§2) |
| **1층** | 완전검증 가능.  고리 무관.  파일·코드 대조로 결판 | **진행 중** (§3) |
| **2층** | 불일치의 **존재**는 완전검증되나 **원인**은 고리 얽힘 | 1층 완료 후 **재작성 예정** |
| **3층** | 고리 얽힘.  1·2층 폐합 전 결론은 잠정 | 전제가 바뀌어 **재작성 예정** |

⚠2·3층은 **지금 쓰면 안 된다** — 없는 잣대를 전제로 쓰게 된다.
근거: `docs/LAYER1_REPLAN_2026-08-07.md` §2.

---

# 2. 0층 — 확보된 자산 (폐기 대상 아님)

계약 10건: `C2-EXEC · H-TRANSFORM · GEN-GUARD · K-SHAPE · K-FRESH · Z-INERT ·
D-BUILD · TE-DEAD · A2-00 · CONFIG-PREC`

Fable 이 추가 이월 판정한 것:
- canonical radiation-field owner(세대·provenance·validity·q-set hash)
- population transaction 원자성(begin/commit/rollback)
- A2-08/09 signed publication + **A2-10 동세대 계약의 내용**(계약은 옳고 위상만 틀렸다)
- 결정론 팔 전체(assemble→ALI→commit) + 해석적 극한 자가검사
- rate-SE 폐합 기계(canonical view Γ 직적분, Seaton+3-body)
- log-sum-exp 이온 사다리 · K-FRESH 개념 · seed-T_e 1회 발행 계약

**계약을 깎지 않는다.**  틀린 것은 계약이 아니라 위상(순서)과 절단이다.

---

# 3. 1층 — 현재 상태

정본 `docs/LAYER1_STATUS_2026-08-08.md`, 작업목록 `docs/LAYER1_REPLAN_2026-08-07.md`.
단 이름 규약 `docs/RUNG_NAMING.md` (**DET-**/**MC-**/**SH-** 접두).

## 3.1 하루 만에 일어난 일

```
08-07 아침  런 시작 불가 (K-FRESH 가 루프 앞의 T_e 를 요구, 발행자는 루프 안뿐)
08-08 새벽  [A2-09][BLOCKED] blocked_stale_line
08-08 오전  [A2-10][BLOCKED] RADEQ_TERM_MISSING
08-08 현재  [A2-10][BLOCKED] RADEQ_NO_BRACKET (DET) / TERM_MISSING (MC)
```
**막히는 곳이 배선에서 물리로 넘어갔다.**

## 3.2 단계별

| 단계 | 계약 | 상태 |
|---|---|---|
| **L1-1** 반복 0 물질 공급자 | 복사장 없는 반복 0 에 물질을 공급하는 선언적 1회 공급자 | **폐합** |
| **L1-2** 세대 장부 위상 정합(=SH-PUB) | commit/view 직후 a208·a209 → A2-10.  실패 시 `(T_e,t)` 보존 | **폐합** `9012995` |
| **L1-3** 결정론 부트스트랩 + 두 팔 합류 | ①세대 단일장부 ②pure a209 ③결정론 line-J̄ | **부분** — ②③ ✓, ①(SH-R5) 미착수 |
| **L1-4** 계측 배선도(3열 어긋남 지도) | J^MC · J^det · J^CMFGEN | 미착수 |
| **L1-5** 물질 입력 물리화 | 시작 상태를 물리로 | **1/5** |
| **L1-5b** 비열적 퇴적 물리화 | 비열 excitation 부재 · 상수효율 처방 | 미착수 |
| **L1-6** 입력축 감사 재개 | 원자데이터 대 CMFGEN 직접대조 | 미착수 |

## 3.3 단(rung)별 상태

| 단 | 소속 | 상태 | 정본 |
|---|---|---|---|
| **SH-PUB** 발행 위상 | 공유 | **폐합** `9012995` | `validation/r7/R7_VERDICT.md` |
| **SH-GAMMA** 감마 침착 소유권 | 공유 | **부분 폐합** `5450518` — Γ2-b 무효·NC2/NC4/Γ4 미실행 | `RUNG_GAMMA_DEPOSITION_OWNER.md` |
| **DET-R6** 결정론 line-J̄ | 결정론 | **부분 폐합** `3ca077d` — R6-4 약함·N6-2/N6-3 미실행·**GPU 빌드 파괴(수리됨 `e32cebc`)** | `RUNG_R6_DETERMINISTIC_LINE_JBAR.md` |
| **SH-GRID** 격자 포함 계약 | 공유 | **부분 폐합** `2e26c2f` — B-6 미측정 | `RUNG_GRID_CONTAINMENT_CONTRACT.md` |
| **MC-EVT** 사건 측도 | **MC 전용** | 적용·빌드·게이트 완료, **판정 초안 대기** | `RUNG_EVENT_MEASURE_LANE_AGREEMENT.md` |
| **SH-RADEQ** 복사평형 잔차 | 공유 | **진단 중** — 감리 진행 | 미작성 |
| **SH-R1** 물리적 seed T_e | 공유 | 발주서만 있음, 잣대 미확보 | `/tmp/claude-10396/codex_grey/ORDER_GREY.md` |
| SH-R5 두 팔 barrier · SH-R9 · SH-R10 | 공유 | 미착수 | 배선도 `validation/layer1_replan/OUT_F*.md` |

## 3.4 오늘 확정된 큰 사실 넷

1. **결정론 팔의 방사성 가열이 항등 0 이었다** — 소비자가 0 을 "할 일 없음"으로 읽었다(SH-GAMMA)
2. **생산자·소비자가 같은 격자인데 중개 격자가 양 끝을 파괴했다** — 1000빈 중 2개가 A2-10 전체를 막았다(SH-GRID)
3. **CPU MC 전송이 기본 구성에서 전멸했다** — `BF_OPACITY=1` 런처 299개 / `EVENT=1` 런처 0개.
   **정직한 쪽(CPU)이 멈추고 조용한 쪽(GPU)이 생산을 계속해 결함이 은폐됐다**(MC-EVT)
4. **선이 흡수만 하고 방출하지 않는다** — `line_abs=1.049e-01`(가열 99.1%) vs `line_emit=0`(SH-RADEQ)

## 3.5 MC-EVT 실측 (판정 대기)

```
수리 전 : 800 패킷 → T03 블록 800 (전멸)
수리 후 : 200 패킷 → T03 블록  11 (reason=OUT_OF_GRID producer=LEGACY_ARGMAX)
[E-ME1] negative_shell_bins=0 / 50000        <- 음수 노출 **0**
[E-ME2] 전 셸 max=0 median=0                 <- 두 생산자 측도 차이 **0**
[A2-10][PRE] lane=MC te=1 rad=1 line=1 opacity=1 emissivity=1 population=1
```
⟹ **MC 팔이 살아났고 결정론 팔과 같은 자리에 도달했다.**
⚠ME1/ME2 가 0 인 것은 **이 덱·이 에폭**의 측정이다.  "언제나 0" 이 아니다.

---

# 4. 앞으로 할 일

## 4.1 즉시 (순서 있음)

1. **MC-EVT 판정 초안 → 감리 → 커밋**  (개정11 절차)
2. **SH-RADEQ** — 감리 결과 대기 중.  1순위 표적 = **`eta_bb = 0` 의 정체**
   - `eta_bb = chi × line_source_S`.  0 이 되는 경로 셋: 전 선 `blocked_source` /
     `line_source_S` 전부 0 / 선 주파수가 방출률 격자 밖 — **아직 미확정**
   - ⚠총평 경고: `NO_BRACKET` 을 "물리적 근 부재"로 읽지 말 것.
     **solver 만 바꾸면 그럴듯하지만 잘못된 `T_e` 를 발행한다**
3. **강등된 단 셋의 미실행 게이트 실행**
   - SH-GAMMA: Γ2-b **위치 결박 byte 비교** · NC2 · NC4 · Γ4
   - DET-R6: R6-4 를 stdout/stderr 분리 byte 비교로 · N6-2 · N6-3
   - SH-GRID: B-6 값 이동 측정(공통 격자 투영·적분 모멘트)

## 4.2 그 다음

4. **SH-R1** — 잣대부터.  CMFGEN caller 의 `FIX_T` 분기를 확인해
   `COMP_GREY_V4` 호출 조건을 밝힌 뒤, 필요하면 `FIX_T=F` CMFGEN 런
5. **SH-R5** 두 팔 barrier → **L1-3 완주**
6. **L1-4** 3열 어긋남 지도(J^MC · J^det · J^CMFGEN) — 캠페인의 실제 계측기
7. **L1-5** 나머지 4건 · **L1-5b** 비열 excitation · **L1-6** 입력축 감사
8. **2·3층 재작성** (1층 폐합 후)

## 4.3 미결 (user 판단 대기)

| # | 항목 |
|---|---|
| 1 | SH-R1 물리적 seed T_e 단일 선언식 — 평면 Eddington 실측 기각(37.4% vs 22.9%) |
| 2 | n_e 수렴 문턱 — 잔차 `1.586e-02` 가 덱 3종 동일 ⟹ **구조적** |
| 3 | catalog 가 `population_atomic_model_sha256` 에 미포함 → GEN-GUARD 침묵 |
| 4 | MC-EVT OFF 경로(legacy argmax) 존폐 — ME2=0 이 나왔으므로 **재검토 필요** |
| 5 | DET-R6 적용범위 30% — CMFGEN 은 전 선 계산 ⟹ 발자국 규약상 **확장이 답** |
| 6 | GPU 가 owner 자신의 해시로 대조하는 비대칭(CPU 는 q-set 해시) |

---

# 5. 업무 분장 (개정11, 2026-08-08)

| 일 | 담당 |
|---|---|
| 발주서 저작 · 가설 수립 · **코딩** | **Codex** |
| **코드 검사** · 실행 · 제출 · 대장 | **운전석** |
| 판정 초안 | 운전석 |
| **★판정 감리 — 단 폐합의 전제** | **Codex (read-only)** |
| 물리 판정 · 인증 · 최종 승인 | **Fable** |

절차 정본 `docs/RUNG_CLOSURE_PROTOCOL.md`.

**왜 감리가 생겼나** — 08-08 하루 데이터:
```
Codex 코딩 결함 4건 → 운전석 검사가 4/4 잡음
운전석 판정 결함 6건 → 운전석이 0/6, 전부 Codex 감사가 잡음
```
편향이 한 방향이다: **엄격한 오판은 스스로 잡고, 관대한 오판은 원리적으로 못 잡는다.**

---

# 6. 문서 안내 (`docs/` 385개 중 현행만)

## 6.1 반드시 읽을 것 (순서대로)

| 파일 | 무엇 |
|---|---|
| **`HANDOVER_2026-08-08.md`** | 이 문서 |
| **`LAYER1_STATUS_2026-08-08.md`** | 1층 현황 — 단계별·단별 상태와 근거 |
| **`LAYER1_REPLAN_2026-08-07.md`** | 1층 작업목록 정본.  "물리가 한 조각씩 서는 순서" |
| **`RUNG_CLOSURE_PROTOCOL.md`** | 단 폐합 절차 + 감리 고정 질문 3개 + **빌드 게이트** + 판정문 금지어 |
| **`RUNG_NAMING.md`** | 단 이름 규약(DET-/MC-/SH-) + 옛↔새 사전 + 배선도 인용 정정 |

## 6.2 단별 사전등록 (착수 전 반드시)

| 파일 | 단 |
|---|---|
| `RUNG_L1_1_BOOTSTRAP_SUPPLIER.md` | L1-1 |
| `RUNG_SEED_TE_PUBLICATION.md` | seed T_e 발행 계약(값이 아니라 발행) |
| `RUNG_GAMMA_DEPOSITION_OWNER.md` | SH-GAMMA |
| `RUNG_R6_DETERMINISTIC_LINE_JBAR.md` | DET-R6 |
| `RUNG_GRID_CONTAINMENT_CONTRACT.md` | SH-GRID (안 A/B 비교와 결정 근거 포함) |
| `RUNG_EVENT_MEASURE_LANE_AGREEMENT.md` | MC-EVT |

## 6.3 캠페인 정본

| 파일 | 무엇 |
|---|---|
| `OUTSIDE_LOOP_POOL.md` (2,108행) | 「고리 밖 감사」 **증거 풀 대장**.  용의자 목록이 아니라 증거 목록 — 성과는 "유죄"가 아니라 **풀에서 제거됨** |
| `VERIFICATION_REGISTERS.md` | **3대 검증 대장**(A 완전검증 / B 검증대상 / C 확정).  회전 리뷰 — 라운드마다 재도전받고 실패 시 강등 |
| `VERDICT_PROTOCOL.md` | 판정 규약 V0–V5.  기계 preflight + 적대검증 스탬프 없이 원장 기재 금지 |
| `CLASSIC_DEBT_CENSUS.md` | 간략화·하드코딩·대충계산 전수 대장 |
| `CLAMP_FIX_PRIORITY_REGISTRY.md` | 클램프 전수조사 |
| `INSTRUMENTATION_DEBT_CENSUS.md` | 계측 부채 |
| `BLOCKER_KFRESH_TE_GENERATION.md` | 08-07 아침의 원리적 불성립 진단 |

## 6.4 인프라·규약

| 파일 | 무엇 |
|---|---|
| `CMFGEN_BUILD_RUN_GUIDE.md` | CMFGEN 자체 런 마스터.  **OMP=16 절대**, grammar slurm 또는 lageunha 수동 |
| `CODEX_CMFGEN_EQUIVALENCE_PLAN_v2.md` | M/E/S 동등 3분류 + 정량 문턱 + 판정 계산 규약 — **검증 계획은 새로 짜지 말고 이것을 상속** |
| `FUV_GRADIENT_ATTACK_DESIGN.md` | falsifier 사다리 F0~F4 |

## 6.5 판정 산출 (`validation/`)

```
validation/r7/       SH-PUB — R7_VERDICT.md · DRIVER_INSPECTION.md · CODEX_R7.md
validation/gamma/    SH-GAMMA — GAMMA_VERDICT.md(정정 포함) · 런 로그
validation/r6/       DET-R6 — R6_VERDICT.md(정정 2회 포함) · nbdiag.log(SH-RADEQ 장부)
validation/gridb/    SH-GRID — GRIDB_VERDICT.md(정정 포함)
validation/evt/      MC-EVT — evt_mc.log
validation/audit/    ★CODEX_AUDIT_2026-08-08.md — 판정 셋을 뒤집은 감사 원문
validation/l1_1_gates/  L1-1 게이트 원 로그
validation/layer1_replan/  OUT_{A..G}*.md — 재배선 파이프라인 산출(배선도 정본)
```

## 6.6 검사 자산 (`scripts/`)

| 파일 | 검사 대상 |
|---|---|
| `check_topion_catalog.py` | 최상단 이온 catalog C1~C8(Z(T)≥g₀ 수학적 하한·vintage 일관성) |
| `check_publication_phase.py` | 발행 위상 P1~P5 — **런 로그만으로** 판정 |
| `check_event_measure_access.py` | MC-EVT NE4 — 접근자 우회 소비자가 없는가 |
| `check_makefile_headers.py` | 헤더 의존성 드리프트 21/21 |
| `t3_cpu_repro.sh` | CPU 재현 하니스.  **`T3_LANE`(MC/DET) · `T3_BIN`(바이너리 지정)** |
| `r6_gates.sh` · `gamma_gates.sh` · `night*.sh` | 단별 게이트 체인 |
| `oracle_compare_cmfgen.py` | CMFGEN 대조(RVTJ·PRRR·ion OUT·GENCOOL fail-closed 파서) |

---

# 7. 인수자가 반드시 알아야 할 함정

오늘 하루에 실제로 데인 것들이다.

| 함정 | 실제 사례 |
|---|---|
| **"빌드 OK" 가 CPU 만 뜻했다** | DET-R6·SH-GRID 두 단 연속.  `.cu`·공유 헤더·API 시그니처를 건드리면 **`make cuda` 도** 돌려야 한다 |
| **`make` 는 최신이면 rc=0 으로 무작동** | 플래그만 바꿔 다시 부르면 무효.  `rm -f <target> && make ...`.  **타임스탬프·sha 로 확인** |
| **env 를 넘겼다 ≠ 그 env 로 돌았다** | 하니스가 런처 env 를 `eval` 로 덮어써 **DET 를 MC 라 불렀다**.  `T3_LANE` 신설로 해소 |
| **그 env ≠ 그 팔의 온전한 구성** | DET 런처 env 로 MC 를 돌려 전송이 전멸했다 |
| **바이트-parity 검사기를 병합 로그에 쓸 수 없다** | 같은 바이너리를 두 번 돌려도 stdout/stderr 끼어듦 위치가 다르다 |
| **다중집합 비교는 byte 비교가 아니다** | 순서·셸 위치를 버린다.  값 교환을 못 잡는다 |
| **주입한 결함의 실재를 먼저 확인** | 안 하면 음성대조가 무효.  "주입했는데 통과" 를 "결함 없음" 으로 읽으면 안 된다 |
| **게이트는 양방향으로 갈려야 자격** | 부재→차단(NC1)과 **정당한 0→통과**(NC3)를 둘 다 시연해야 한다 |
| **판정 기준 = 단 경계** | 넓히면 남의 단의 실패를 내 단의 실패로 적는다(오늘 2회) |
| **빌려온 계약은 원래 양의 불변식을 데려온다** | 침착률을 온도 매니페스트로 해싱 → `te>0` 이 따라와 **모든 덱에서 발행 불가** |
| **`pgrep -f` 자기매칭** | 오늘까지 4회.  PID 기반 `kill -0` 을 쓴다 |
| **`run_in_background` + `nohup &` 겹치면 죽는다** | `make cuda` 가 컴파일 도중 끊겼다.  memory `reference_background_launch_patterns` |

---

# 8. 실행 환경

| 티어 | 규칙 |
|---|---|
| 로그인 노드 | **연산 실행 금지**(빌드는 예외).  파일 조회·grep·판독만 |
| 경량 오프라인 | grammar-debug (nested ssh) 또는 slurm job-per-run |
| **대형 오프라인** | **lageunha 직접 투척**(OMP=60).  투척 전 `ssh lageunha uptime` 으로 부하 확인 |
| **CMFGEN** | grammar CPU slurm 또는 lageunha 수동.  **OMP=16 절대** |
| GPU 생산 런 | slurm job-per-run, h200→h100 (full-NLTE 80GB — a40 제외) |
| grammar 제출 | `--exclude=grammar072,grammar078,grammar080` 기본 |

★`git add -A` 금지(미추적 1,500·validation 452MB).  커밋·푸시는 운전석만.
★덱·`/gpfs` 정본 불변.

---

# 9. 한 문장 요약

**어제까지 런이 시작조차 못 했고, 오늘 네 개의 배선 결함을 폐합해 두 팔이 모두
복사평형(A2-10)까지 도달했다.  이제 처음으로 물리 자체를 볼 수 있고, 첫 관측이
"선이 흡수만 하고 방출하지 않는다" 이다.  그 원인 규명이 다음 일이다.**

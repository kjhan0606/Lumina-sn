# 발주서 L1-A 검수표

감사 대상만 읽기 전용으로 검사했으며, 발주 내용이나 재측정은 실행하지 않았다.

| 항목 | 판정 | 실측 근거(명령과 출력) | 발주서 수정 요구 |
|---|---|---|---|
| **1. A1 7개 열의 충전 가능성** | **반박** | `sha256sum .../{levels.csv,line_list.csv,cmfgen_sigma_bf.bin}` → 구 덱 `01492367… / 2a0b5f9f… / 7135be60…`, `_ftos` `17865d5f… / b20069e5… / e38e80a7…`.<br><br>`sha256sum /gpfs/.../lumina_cuda.withParityAH` → `bcb1292707d33d324763b0ca9132087fc5081416801b59b8a08389b5b312dc44`.<br>`stat` → binary `2026-08-02 12:33`; `src/lumina_atomic.c` `08-03 22:18`, `src/lumina_cuda.cu` `08-03 01:19`. 즉 **binary SHA 자체는 확인되지만 그 바이너리를 만든 source snapshot은 미확립**이다.<br><br>`find .../instr_capture_188932 -maxdepth 1 \\( -iname '*sha*' -o -iname '*manifest*' \\)` → payload manifest 5개와 fluor SHA뿐. 같은 명령을 CMFGEN 런 디렉터리에 적용 → **출력 없음**. 측정시점 CMFGEN 런 전체 SHA manifest가 없다.<br><br>`cmp`로 구/현 덱 companion 대조 → `config.json, geometry.csv, density.csv, abundances.csv, deposition_cmfgen.csv, electron_densities.csv, plasma_state.csv` 전부 `SAME`.<br><br>**[추론 표시]** 원자자료 의존 13개 `I1,I2,I2a–d,I3,I3a–c,I4,I5,I17`은 `STALE`; I6은 관련 companion byte 기준 `CURRENT`; I7–I10은 binary/source·코드 epoch가 섞여 `UNKNOWN` 4개다. 따라서 `epoch_status`만 보면 **14개 충전(13 STALE+1 CURRENT), 4 UNKNOWN**. 그러나 엄격한 “7열 완전 충전”은 측정시점 CMFGEN snapshot과 build attestation이 없어 **0/18**, 적어도 한 `UNKNOWN`이 있는 행은 **18/18**이다. | ① `epoch_status` 한 칸을 `deck_epoch / binary_epoch / source_epoch / cmfgen_run_epoch / atomic_tree_epoch`로 분해하라.<br>② `binary_sha` 옆에 `source_tree_sha`, dirty diff SHA, build command, compiler/CUDA/link-library, build environment manifest를 추가하라. 현 7열로는 I15를 해소하지 못한다.<br>③ `UNKNOWN`과 별도로 `NOT_APPLICABLE`을 추가하라. 오프라인 비교에 바이너리 SHA를 억지로 붙이면 “측정에 사용”했다는 허위 계보가 된다.<br>④ A1을 ID당 한 행이 아니라 **ID×specimen(epoch)** 행으로 바꾸고 구 덱과 `_ftos`를 별도 행으로 보존하라.<br>⑤ `cmfgen_snapshot`은 3.2 GB 런 디렉터리 전체라는 모호한 표현 대신 ID별 실제 소비 파일 allowlist와 manifest SHA를 요구하라. |
| **2. A2 ID별 재측정 가능성** | **반박** | `levels.csv` 전수 집계 → 구 덱 `26,592 levels / 53 ions`, `_ftos` `31,792 / 59`; old-only ion `0`, ftos-only=`14,4 · 26,5 · 27,4 · 27,5 · 28,4 · 28,5`.<br><br>그러나 collision manifest 비교는 다음과 같다:<br>`14,3 20,3 27,1 27,3 28,1 28,2 28,3 OLD=OK FTOS=SKIP`.<br>즉 Si IV, Ca IV, Co II·IV, Ni II–IV의 **충돌표 7종은 구 덱에만 있다**. `_ftos`에는 해당 이온은 있지만 CMFGEN 현 vintage가 “0 tabulated transitions”라 바이너리가 없다. Fe VI `(26,5)`만 새 `OK`다.<br><br>선 수 실측: Fe IV `4,336→72,223`, Ni IV `4,199→72,898`, Co IV `4,041→69,425`, Fe III `136,263→136,263`.<br>σ header: `od -An -t d4 -N16` → 구 `(magic,1,26592,1000)`, 현 `(magic,1,31792,1000)`. 기존 감사 근거는 present `26087→31237`; Fe IV/Ni IV flag `1000/1000`. 다만 CMFGEN type `2/3/8` 2,084레벨 평가기는 아직 없다.<br><br>ID별 가능성: **가능 14개**=`I1`(단 수치가 아니라 현재 `NO-COUNTERPART` 상태 census), `I2,I2a–d`, `I3,I3a–c`(지원 PHOT type으로 범위 제한), `I4,I6,I7,I17`; **부분 3개**=`I5`(DR 설정 가능/RR 불가), `I9`(기록 환경만 가능/바이너리 전체 의미론 불가), `I10`(deposition bytes 동일/코드 epoch 미확인); **현재 불가 1개**=`I8`(동일 속도좌표 CMFGEN `L(r)` 추출기 부재). | ① “이온 존재”와 “비교량 존재”를 분리해 `ion_present / quantity_present / counterpart_present / evaluator_supported`를 기록하라.<br>② `STALE`만이 아니라 `UNKNOWN`과 `PARTIAL`인 ID도 A2 대상에 넣어라.<br>③ I1은 구 4,455표의 숫자 재측정과 현 `NO-COUNTERPART` census를 다른 metric으로 분리하라.<br>④ I3 계열에는 지원 PHOT type 목록과 unsupported 2/3/8 분모를 명시하라.<br>⑤ I5를 v1 요구대로 `I5a DR`과 `I5b RR/Milne`로 분리하라.<br>⑥ 분자/분모가 자연스럽지 않은 I4·I6–I10에는 배열·범위·카테고리 비교용 별도 계약을 둬라. |
| **3. A3 4축의 18개 ID 피복** | **반박** | ID 열거 명령:<br>`printf '%s\\n' I1 I2 I2a I2b I2c I2d I3 I3a I3b I3c I4 I5 I6 I7 I8 I9 I10 I17 \| nl -ba` → `1…18`.<br><br>시험 배정에서 막히는 사례:<br>`I2d = WELL / MATCH / NUMERIC / ?` — 구 epoch에서 제거된 항목에 `CLOSE/NONE` disposition이 없다.<br>`I8 = UNVERIFIABLE / ? / DEFINITION / DEFINE` — 검증 전에는 outcome의 `NOT-ASSESSED/INCOMPARABLE`이 없다.<br>`I17 = WELL / RESOLVED / COVERAGE / ?` — 역시 `CLOSE`가 없다.<br>`I5 = UNVERIFIABLE / PARTIAL / {DEFINITION,PROVENANCE} / {DEFINE,REMEASURE}` — kind와 disposition이 단일값인지 다중값인지 정의가 없다.<br>또한 I17의 현 PASS와 stale `verification.log` 실패를 네 축 어디에도 표현할 수 없다. | ① `outcome`에 `NOT-ASSESSED`, `INCOMPARABLE`을 추가하라.<br>② `disposition`에 `CLOSE`와 `NONE`을 추가하라.<br>③ `kind`·`disposition`의 cardinality를 명시하고 복합 사례는 배열을 허용하라.<br>④ 다섯째 축 `evidence_status = VALID / STALE / FAILED / MISSING / MIXED`를 추가하라.<br>⑤ 18개 ID의 잠정 배정표 자체를 A3 필수 산출물로 고정하고, 빈 값·임의 `PARTIAL` 대체를 스키마 오류로 처리하라. |
| **4. A4 9종 함정과 기계검사 스키마** | **반박** | 발주서 #8은 문언상 “`missing / zero / unsupported` 네 상태”라고 쓰면서 실제로는 **3개만 열거**했다. A5에서는 뒤늦게 `present`를 포함한다.<br><br>9종으로 잡히지 않는 오늘의 사례가 남는다: ① I7의 **계산 역할/파이프라인 단계 불일치**(`1000↔196185`), ② I17의 stale 실패 로그 및 G7처럼 **검사기 신선도·비공허성·음성대조** 문제, ③ 12→13 및 누락 5건처럼 **대상 universe 열거 완전성**, ④ binary/source SHA를 나란히 적기만 하고 실제 build 관계를 증명하지 않는 **build attestation 결손**.<br><br>`wc -l .../verification.log; sed -n '1,20p'` → `1` / `ERROR: R4 verifier contract failure: 'NoneType' object has no attribute '__dict__'`. | 9종을 최소 13종으로 확장하라: 기존 9종 + `role/stage`, `universe/cardinality`, `evidence freshness/non-vacuity/negative-control`, `build attestation`.<br><br>스키마는 단순 체크박스가 아니라 비교 레코드별 typed object여야 한다. 필수 필드: `left/right{role,authority,consumed_path,resolved_path,sha256,epoch}`, `universe{selection,denominator,cardinality}`, `coordinate{frame,unit,range,interpolation,extrapolation}`, `sampling{rule,alternatives}`, `precision{digits,threshold_mode}`, `join{keys,normalization,multiplicity,duplicate_policy}`, `states{present,missing,zero,unsupported}`, `error{absolute,relative,ulp,zero_denominator}`, `evidence{producer_sha,command,exit_code,validator,created_at,negative_control}`.<br>교차 제약으로 4상태 합=분모, sampling-sensitive이면 대안 ≥2, ULP이면 dtype 필수, universe ID=등록된 18개 등을 검사하라. A4는 전역 “헤더”가 아니라 **ID/metric마다** 기록해야 한다. |
| **5. A5 재현기 작업량·메모리·시간** | **반박** | 파일 실측:<br>구 line CSV `390,625,345 B / 2,584,132행`, `_ftos` `334,306,916 B / 2,220,953행`; 합계 `724,932,261 B`, `4,805,085행`.<br>구 σ `212,762,624 B`, 현 σ `254,367,824 B`; 합계 `467,130,448 B`이며 raw double payload는 58,384,000점이다. collision regular binaries는 구 `18,662,868 B`, 현 `21,568,164 B`.<br><br>`/usr/bin/time -v awk -F, ... line_list...` → `rows=4805085 positive_A=4805085`, wall `3.15 s`, max RSS `19,356 KB`.<br>`time sha256sum` line CSV 두 개 → `0.72 s`, RSS `3,072 KB`; σ 두 개 → `0.50 s`, RSS `3,072 KB`. 이는 캐시된 **I/O 하한**이지 결합·CMFGEN 평가 시간은 아니다.<br><br>**[추론 표시]** σ를 전량 배열로 동시에 읽으면 raw만 약 `445.5 MiB`; 중간 mask/relative-error 배열까지 만들면 1 GiB를 넘을 수 있다. 1,024레벨×1,000점×양쪽 double chunk는 약 `15.6 MiB`라 streaming/memmap이면 RSS 100 MiB 이내 설계가 가능하다. Python object dict로 88만 결합키를 만들면 수백 MiB 이상 가능하므로 compact structured key나 외부 정렬이 필요하다. 실제 evaluator 시간은 재현기 부재로 **미결**이다. | 하나의 CLI 진입점은 가능하지만 구현은 최소 **collision / line-identity·A / sigma·RR** 세 엔진으로 분리하라. I6–I10은 별도 `config` 엔진이 필요하다. 엔진별 실패가 다른 수치의 출력을 오염시키지 않게 하라.<br><br>필수 자원 계약: peak RSS, wall time, chunk 크기, 지원 PHOT type, 입력 행수/바이트/SHA, 처리·미지원 합계. 전량 load 금지, σ는 memmap/chunk 방식. 1% fixture와 full-run benchmark를 둘 다 요구하라.<br><br>v1이 요구한 `command / version / 예상 분모 / checksum`을 복구하고, `880406·3953894`를 코드에 하드코딩하지 말고 golden manifest의 예상값으로 검사하라. `exact`와 `ULP`는 일반 임계 인자가 아니므로 dtype·endianness·ULP distance 규칙을 별도 정의하라. |
| **6. B·C·D 선행조건으로서 충분성** | **반박** | A1 실측에서 strict 완전 계보 `0/18`, A2에서 collision 7종 current counterpart 소실, A3의 outcome/disposition 공백, A4의 evidence/build 함정 누락, A5의 evaluator·golden contract 부재가 확인됐다.<br>또한 v1은 `I12 level/line identity`를 provenance 선행 게이트로 요구했으나 L1-A는 결합키를 “기록”할 뿐 identity gate를 실행·통과시키지 않는다. | 현 L1-A 완료만으로는 B를 발주할 수 없다. B 전 최소 추가조건은 ① canonical current specimen 선언, ② component별 epoch matrix, ③ I12 level/line identity gate, ④ binary build attestation 또는 binary-emitted semantics manifest, ⑤ current collision `SKIP` 정책, ⑥ 재현기 golden/음성대조/자원 게이트, ⑦ I17의 `gate_ftos + verify_deck_r1_vintage` 명시 실행계약이다.<br>C 전에는 point/average bake semantics와 PHOT evaluator 지원범위, I5a/b 분리가 추가로 필요하다. D 전에는 공통 좌표·외삽 금지·동일 역할 대응표와 CMFGEN `L(r)` 추출 계약이 필요하다. |
| **7. v1 권고의 누락·왜곡** | **반박** | `rg -n`으로 v1 수정 요구를 대조한 결과:<br>• `I5를 **I5a ... I5b**로 분리` — L1-A ID 목록에는 I5 하나뿐.<br>• `I6을 시간/공간범위/속도/밀도로 분해` — L1-A에 하위 metric 계약 없음.<br>• `I9를 ε clamp/외부 반복/Λ 반복/damping/종료조건/온도탐색으로 분해` — 반영 안 됨.<br>• `I12의 level/line identity 부분은 ... provenance gate` — 반영 안 됨.<br>• 재현기 `명령, version, 예상 분모와 checksum` — version·예상분모·golden checksum 누락.<br>• I17 `gate_ftos + verify_deck_r1_vintage` — 일반 A5로 축약되어 명령·stale log 처분 누락.<br><br>반면 18개 ID, 네 판정축, 9종 함정의 표면 목록은 옮겨졌다. | v1 요구를 다음과 같이 복원하라: I5/I6/I9 세분, I12 identity gate, binary/build/env 완전 계보, 구/현 epoch 별도 행, I17 두 verifier와 stale 로그 처분, 재현기 version·예상 분모·checksum·negative control.<br><br>또 “A1이 I15·I16 해소를 겸한다”는 문장은 왜곡이다. 현재 A1은 I16의 경로 해석 일부만 다루고 I15의 source/build/toolchain/environment는 해소하지 못한다. “부분 해소”로 고치고 남은 I15 계약을 명시하라. |

반려
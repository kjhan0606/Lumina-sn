# 발주서 C·D·E — 조성 동일성 복원 + 입력 판독 fail-closed

캠페인 「고리 밖 감사」 **층 0**. 정본 대장 `docs/OUTSIDE_LOOP_POOL.md`.
**v4 (2026-08-03)** — v1·v2·v3가 Codex 검수에서 연속 **반려**됐고, 반박 22건을
운전석이 실측 재확인해 반영한 판이다. 반려 사유와 정정 내역은 §11.

**v4의 성격 변화**: **발주 E의 정량화가 완료됐다**(§2.7 — 운전석 실측).
Lumina와 CMFGEN은 서로 다른 StaNdaRT 파일에서 조성을 얻지만, **그 차이는
불투명도 지배 원소에서 ≤3.33%, 밀도 0.07%**다. 조성은 캠페인의 배수급 불일치를
설명하지 못한다 ⟹ **조성은 원인 풀에서 제거**되고, 지난 코퍼스는 조성 때문에
무효화되지 않는다. 남는 실작업은 **D(판독기)와 C(생산 경로)뿐**이다.

---

## 0. 이 발주서의 지위

2026-08-03 운전석 오류는 **전부 발주서·전제 단계**에서 나왔고 구현 단계는 0건이다.
검수 관문을 구현이 아니라 발주서에 둔다. 두 차례 검수가 이 방식을 입증했다:

- v1 반박 3건 중 **2건이 정상 덱을 죽이는 게이트**였다(D5·D6을 FATAL로 지정).
- v2 반박 4건 중 하나는 **`G8 = 9/9`가 산술적으로 불가능**하다는 것이었고,
  하나는 **헤더만 있고 데이터 행이 0개인 파일이 전 FATAL을 통과**한다는 반례였다.

구현했다면 두 번 다 하루를 버렸다.

---

## 1. 계약 (수리 단위, 순서 고정)

| 발주 | 계약 진술 | 대상 | 성격 |
|---|---|---|---|
| **D** | **입력 판독은 fail-closed다. 선언된 형상과 어긋나는 입력은 조용히 통과하지 못한다.** | `src/lumina_atomic.c` | 탐지기 |
| **C1·C2** | **덱의 조성은 모델 입력이다. 원자데이터 파이프라인은 조성을 생산하지 않는다.** | `scripts/` | 생산 경로 |
| **C3** | 결함 덱 3기에 **현 정본** 조성 적재 — 계보 일관성 복원 | `data/` | 적재 |
| **E** | **Lumina 정본과 CMFGEN 입력의 조성 차이를 정량화한다.** 수리하지 않는다 | 감사 + provenance 반입 | 정량화 |

**순서 = D → C1·C2 → C3 → E.**

**v2에서 순서가 바뀌었다.** v2는 `C3 ← E`(인증 후 적재)였으나, §2.6에서 두 출처가
다름이 **이미 확정**됐으므로 E는 더 이상 C3의 선행조건이 아니다. C3는 "CMFGEN과
동일한 조성"을 만들지 못한다 — 그건 별건 J의 몫이다. C3가 하는 일은 **오늘 밤
파괴된 세 덱을 6/29 정본 상태로 되돌리는 것**뿐이며, 그것은 E와 독립이다.

- D가 먼저인 이유: D 없이는 조성 결함이 **런타임 무증상**이다. 탐지기를 먼저 세운다.
- src 편집 태스크는 한 번에 하나(규약)이므로 D는 단독으로 나간다.

---

## 2. 실측 근거 (2026-08-03. ★= Codex 검수 산출)

### 2.1 결함 산출물

```
_ftos / _fullcov / _links  의 abundances.csv
  형상 : 30 셸 × 15 원소   (geometry.csv 는 세 덱 모두 50행)
  Σ_Z X: 0.949930 전 셸 동일
  층화 : 없음 — 15 원소 전부 전 셸 동일값
★ D2(헤더 열수 ≠ geometry) 위반은 저장소 전체에서 정확히 이 3파일뿐이다.
```

### 2.2 정본

```
data/tardis_reference_toy06_19p48d/abundances.csv   (2026-06-29 14:54:10, 2808 B)
  형상 : 50 셸 × 8 원소
  Σ_Z X: double 순차합 max|Σ−1| = 2.220446049250313e-16  (1e-12 위반 0)★
  층화 : Ni/Co/Fe 비영 12셸, Ca/S/Si 비영 46셸
  원소 : {28,27,26,20,16,14,8,6}, O(8)·C(6)는 전 셸 정확히 0.0
```

**⚠ 같은 덱의 `atom_masses.csv`는 15원소.** 누락 Z = `12,13,21,22,23,24,25`.
**이것이 정상 상태다** — D5를 FATAL로 걸면 정본이 죽는다(v1의 오류).
`DEFAULT_ABUNDANCES`의 15원소가 이 목록과 정확히 일치한다: 자리표시자가
`elements_used`(원자데이터 원소 목록)를 그대로 따라 쓴 결과.

### 2.3 결함 생산 경로 (코드 폐합)

| # | 위치 | 내용 |
|---|---|---|
| 1 | `expand_atomic_data_cmfgen.py:300` | `N_SHELLS = 30` 하드코딩 |
| 2 | `:301-305` | `DEFAULT_ABUNDANCES` 15원소 균질 자리표시자 (합 0.94993) |
| 3 | `:918-925` | `write_abundances()` — 전 셸 동일값 |
| 4 | `:1717` | `main()`에서 무조건 호출 |
| 5 | `deck_regen_fullcov_driver.py:24` | `abundances.csv` ∈ `REBUILT` |
| 6 | `:58` | `copy_companions()`가 `REBUILT` 건너뜀 ⟹ 정본 미복사 |

5·6이 정본 복사를 막고 1–4가 자리표시자를 써 넣었다. 셸 수 30→50 확장 때
이 파일이 안 고쳐진 것이다(주석 `:826`도 `shell29`로 잔존).

### 2.4 판독기 fail-open (발주 D 근거)

`src/lumina_atomic.c:817-842`. **★= v2 검수 신규**

| 줄/ID | 무증상 실패 | 확증 |
|---|---|---|
| `:820` | `if (fp)` — 파일 부재 시 조용히 통과, 전 조성 0.0 | 소스 |
| `:823` | 헤더 `fgets` 반환값 미검사, **헤더 내용 자체를 버림** | ★재현 |
| `:835-838` | 열 30개인데 `s < n_shells`로 50회 순회 | ★최소재현 확증 |
| `:834` | `atom_masses.csv`에 없는 Z 조용히 폐기 | 소스 |
| `:825` | **`elem_idx < n_elements`에서 멈춤 — EOF까지 안 읽음** | ★ |
| `:822` | `char line[8192]` 초과 시 조용한 절단 | ★ |
| — | NaN·Inf·음수·후행 쓰레기·NUL 바이트 무검사 | ★ |
| — | 중복 Z 무검사 (abundance 측·`atom_masses` 측 **양쪽**) | ★ |
| — | **헤더 셸 순서 역전이 무증상** — 역순 헤더도 동일 결과 적재 | ★반례 |
| — | **데이터 행 0개(헤더만)가 전 FATAL 통과** — 전 조성 0인데 종료 0 | ★반례 |

**열 순회 확증** (★, v1에서는 운전석 추론이었음):
```
shells_30_49=[0.0 × 20]   pointer_advances_28_33=[4,4,0,0,0,0]   all_outer_zero=True
```
⟹ `_ftos` 덱으로 런을 던졌다면 **외곽 20셸이 빈 채로 경고 없이** 스펙트럼이 나왔다.

**헤더-only 반례가 특히 중요하다**: D1의 근거는 "0 조성은 어떤 경우에도 정당하지
않다"인데, 파일이 존재하되 본문이 없으면 D1은 안 걸리고 결과는 똑같이 0 조성이다.
D1만으로는 계약이 안 닫힌다.

### 2.5 CMFGEN 측 권위 사슬 (★ 검증 완료)

```
StaNdaRT 19.48d 스냅샷
  /gpfs/kjhan/cmfgen_runs/toy06_19.48d/snia_toy06_19.48d.dat
  ★ 파일 859행 = 주석 포함 총행 / 데이터 zone 807개 / 전 zone 21열
  ★ 속도 전 범위 25.0 – 40,325.0 km/s
        ↓ mk_sn_hydro.py  (동 디렉터리, 우리가 작성)
          v∈[1000,36000] 절단 → ★ below_1000=20, selected=700, above_36000=87
          outer→inner 역순
          ★ IGE·56Ni 에 floor 1e-10 주입 (":23-24", 주석 "so consistency check passes")
          ★ 6원소 질량분율을 Σ=1로 재규격화 (":27-29")
  toy06_19.48d/SN_HYDRO_DATA
        ↓ ★ sha256 동일 (fba01d5b…d9ec)
  toy06_19.48d_jnu4/SN_HYDRO_DATA
     자기선언: data points 700 / mass fractions 6 / isotopes 3 / t=19.4800000 d
```

**700은 속도 절단 후 남은 원본 zone 수이며 Lumina의 50셸과 무관하다.**

**확정**: CMFGEN은 6원소로 계산한다 — 입력파일의 자기선언.
**폭로**: 변환기는 **우리 것**이고 그 안에 floor·재규격화가 있다. "CMFGEN 입력 =
순수 StaNdaRT"는 **거짓**이다. 규약상 금지 clamp가 벤치마크 입력단에 있다 ⟹ 별건 H.

### 2.6 ★★★ Lumina 측 계보 — **출처가 다르다 (확정)**

v2 검수의 최대 산출. 운전석이 "미확인"으로 남긴 공백이 **닫혔다.**

```
정본 생성기 = scripts/build_toy06_epoch.py     ★특정
  mtime 2026-06-29 14:53:58  vs  정본 덱 14:54:10  (11초 선행)
  기본 출력 경로 = 정본 덱
  ★ 알고리즘 메모리 재현 결과:
       generated_text_bytes=2808 / actual_bytes=2808
       byte_equal=True / array_max_abs_diff=0.0
  직접 입력 = data/standart_data1/input_models/snia_toy06_1h_lowres.dat
              source_shape=(202, 21), source_tend_days=0.041667
  변환      = 자체 Bateman 붕괴 (0.041667 d → 19.48 d)
              + 중심점 선형보간, i_phot=19, v 3900–40300, 50셸
```

| | Lumina 정본 | CMFGEN |
|---|---|---|
| StaNdaRT 원본 | **1시간판** 202 zone | **19.48일판** 807 zone |
| 19.48d 조성 획득 | **자체 Bateman 붕괴** | StaNdaRT가 이미 적용 |
| 재격자 | 중심점 선형보간 → 50셸 | 속도 절단 → 700점 |
| 속도 범위 | 3,900 – 40,300 | 1,000 – 36,000 |

**⟹ 두 코드는 서로 다른 파일에서, 서로 다른 경로로 조성을 얻는다. 동일성 불성립.**

★피복 실측 (CMFGEN 계산격자 대 Lumina 50셸):
```
full_coverage_shells=44 (index 0–43)
partial_coverage=[(44, 0.0917)]        ← 셸 44는 9.17%만 피복
zero_coverage_shells=[45,46,47,48,49]  ← 5셸 완전 무피복
```

**주의**: 무피복은 **CMFGEN 계산격자**의 한계이지 StaNdaRT 원본의 한계가 아니다.
19.48d 스냅샷은 40,325 km/s까지 있으므로 Lumina 전 50셸을 덮는다.

**"실행 명령 기록은 없다"** — 생성기 특정은 mtime·기본출력경로·바이트동일성에 근거한
**강한 추론**이다(Codex 자기표시). 알고리즘·직접입력의 특정에는 충분하나,
"이 스크립트가 실제로 실행됐다"는 로그로 확증되지 않았다.

### 2.7 차이의 크기 — ⚠**판정부 철회됨** (수치는 유효, 결론은 무효)

> **2026-08-03 후속 정정 (정본 = `docs/OUTSIDE_LOOP_POOL.md` 0-C4·0-C5)**:
> 아래 수치 5종은 Codex 독립 재계산으로 소수점까지 재현됐으나 **"raw 700점
> 중심보간·완전피복 44셸" 조건부**다. CMFGEN 실제 90-depth 격자 대조 시 major
> 67.13%(질량가중 78.08%) ⟹ **"조성 = 풀에서 제거" 판정은 철회, 미결 복귀.**
> 아래 판정 블록의 "n_e 1.92×" 인용도 무효 — 그 값은 07-23 Dig A가 기각한
> 잣대 사고(⟨Z⟩=1 placeholder)로, 수렴비 실물은 0.95–1.02다(0-C5).

(원문 보존 — 철회 경위의 증거로 남긴다)

§2.6에서 출처가 다름이 확정됐으므로 **차이가 얼마인지**를 쟀다. 방법: 정본
`geometry.csv`의 고정 50셸 중심 속도에 `SN_HYDRO_DATA`의 6원소 질량분율을
선형보간해 정본 `abundances.csv`와 직접 대조. 격자를 건드리지 않는다.

```
CMFGEN  : 700점, v = 1025.0 – 35975.0 km/s, Σ_species X ∈ [0.9999999988, 1.0000000010]
Lumina  : 50셸,  v = 3900.0 – 40300.0 km/s
피복    : 완전 44셸(0–43) / 부분 1셸(44, 5.91%) / 무피복 5셸(45–49)
```

**불투명도 지배 원소 (X_cmf > 1e-3)**
```
max rel = 3.33%   (Si, 셸 4, v=7176 km/s, X_lum 2.2366e-2 vs X_cmf 2.1644e-2)
셸 4 전 원소:  Si +3.33%  S +3.33%  Ca +3.33%   |   Fe −0.14%  Co −0.14%  Ni −0.14%
               └─ IME 3원소가 함께 +3.33%, IGE 3원소가 함께 −0.14%
                  ⟹ 전이층에서의 IME/IGE 분배 차 + 각자 재규격화의 결과
max |ΔX| = 1.0379e-3  (Co, 셸 4, X=0.76 ⟹ 상대 0.14%)
```

**미량 원소 (X_cmf 1e-4 – 1e-3)** — IME 영역의 IGE 꼬리
```
max rel = 63.21%  (셸 11, v=12272 km/s)
   Fe 6.4075e-5 vs 3.9258e-5   |   Co 5.1914e-4 vs 3.1808e-4
지수적 급감 구간이라 202존 선형보간 vs 807존 원자료의 차가 크게 벌어진다
```

**floor 아티팩트 분리**
```
X_lum == 0 且 X_cmf ≤ 2e-10 인 (원소,셸) 쌍 = 108   ← mk_sn_hydro.py 의 floor. 실차 아님
그 밖에 차이가 있는 쌍                      =  60
```

**밀도 (독립 교차검증)**
```
max rel Δρ = 0.070%  (셸 40, v=33384)   median = 0.0456%
```

### ★ 판정

| | |
|---|---|
| 사전등록 절대 임계 `1e-3` | max 1.0379e-3 — **4% 초과**(Co, X=0.76 ⟹ 상대 0.14%) |
| 사전등록 상대 임계 `1%` | 지배원소 3.33% — **초과**. 미량원소 63.21% — 초과 |

**임계는 초과했으나 배수급이 아니다.** 캠페인이 설명하려는 불일치는
`n_e` 1.92× · `u` EUV 5.13× · FUV 2.53× · s8 Fe 4.249× · Co 17.255×다.
**3%의 조성차가 5배의 복사장 차를 만들 수 없다.** 밀도가 0.07%로 일치하는 것이
재격자 기계가 건전함을 독립적으로 뒷받침한다.

**⟹ 조성은 캠페인 불일치의 원인 풀에서 제거된다.**
**⟹ 지난 코퍼스는 조성 때문에 무효화되지 않는다.**

**단, 동일성은 성립하지 않는다** — 3.33%(전이층)·63%(IGE 꼬리)·floor 108쌍은
실물이다. user 기준(동일성)에서는 잔여 항목이며 별건 J로 남는다. 다만 **강제 사항이
아니라 정밀화**이고, 재측정 발주를 요구하지 않는다.

⚠ **운전석 자기정정**: 이 측정 직전에 운전석은 "CMFGEN을 진리로 놓고 잰 모든 수치가
살아남지 않는다"고 보고했다. **재기 전에 오염을 단정한 것이며 철회한다.**
규약 `feedback_audit_the_yardstick_first`("X=N배는 가설, 분모 실측 증명 먼저") 위반이다.

---

## 3. 이 발주의 범위 밖 (풀에서 빼지 않음)

| ID | 내용 |
|---|---|
| **F** | 격자 범위 불일치: CMFGEN 계산격자 1,000–36,000 vs Lumina 3,900–40,300. 셸 44 부분·45–49 무피복 |
| **G** | 준위 절단(`MODEL_SPEC` NF/NS 대비 10/27 이온 초과), R3c 충돌 폴백, I18 역잉여 |
| **H** | `mk_sn_hydro.py`의 floor 1e-10 · 재규격화 — 벤치마크 입력단 clamp. **신규 등록** |
| **I** | `atom_masses.csv` 15원소 vs 모델 6원소 — 9원소가 X=0으로 실린 비용 |
| **J** | **조성 출처 통일 여부** — Lumina를 19.48d 스냅샷 출처로 옮길 것인가. **정본 교체는 전 계보의 비교가능성을 끊으므로 user 결정 사항.** E의 정량화 이후 별건 발주 |

**F·G·H·I·J가 열려 있는 한 "조성 동일성 확보"라고 쓰지 않는다.**

---

## 4. 발주 D — 판독기 fail-closed (src 단독, 최우선)

`src/lumina_atomic.c` 조성 판독부. **등급(FATAL/WARN) 구분 필수.**

### 4a. FATAL — 비영 종료 (**11개 ID**)

| ID | 조건 | 근거 |
|---|---|---|
| D1 | `abundances.csv` 부재 | 조용한 0 조성은 정당화 불가 |
| D2 | 헤더 열 수 ≠ `n_shells` | ★ 저장소 위반 = 결함 3덱뿐 |
| D3 | 데이터 행의 실제 필드 수 ≠ `n_shells` | ★ 위반 = 결함 3덱뿐 |
| D4 | CSV의 Z가 `atom_masses.csv`에 없음 | ★ 저장소 위반 0건 |
| D7a | 질량분율 NaN | ★ 0건 |
| D7b | 질량분율 Inf | ★ 0건 |
| D7c | 질량분율 음수 | ★ 0건 |
| D8 | 수치 파싱 후 잔여 비공백 쓰레기 · NUL 바이트 · **Z 토큰 후행 쓰레기** | ★ 0건 |
| D9 | 중복 Z — **abundance 측과 `atom_masses` 측 양쪽** | ★ 0건(양쪽) |
| D10 | 행 길이가 버퍼를 넘어 절단됨 | ★ 저장소 최대 실물행 1,499 B |
| **D12** | **헤더 스키마 ≠ `atomic_number,0,1,…,n_shells-1` (순서 포함)** | ★ 역순 헤더 무증상 반례 |
| **D13** | **인식된 abundance 데이터 행 0개** | ★ 헤더-only 반례. D1로 안 잡힘 |
| **D14** | `atom_masses.csv`의 비정수/비양수/표현범위 초과 Z · 비양수·비유한 mass · 행 수 불일치 | ★ 923파일 전부 0건 |
| **D15** | **셸별 `Σ_Z X ≤ 0`** | ★ 인식행 1개가 전부 0이면 D13도 통과. 저장소 min 0.6919, 비양수 0건 |
| **D16** | **개별 `X > 1`** | ★ `X=1.2`가 전 FATAL 통과. 저장소 max 정확히 1.0 |
| **D17** | **`strtod`/`strtol` 후 `errno == ERANGE`** | ★ `strtod("1e-9999")`→0.0(errno 34), `strtol("4294967302")`→int cast 6 = **Z=6 위장** |

⚠ **D12–D17은 v3·v4 신규**이며 저장소 전수 검사에서 **위반 0건**이다.
정상 덱을 죽이지 않는다 — Codex 전수 확인:
```
D12_vs_geometry ok=906 bad=3(결함 3덱) no_geometry_preempts=11
D13 zero_body=0 zero_recognized=0
D14 bad_header/fields/Z/mass/rowcount/dupZ = 전부 0 (923파일)
CORPUS max_X=1.0  min_shell_sum=0.69191  nonpositive_shell_sums=0
atom_mass_Z_minmax=6 28  nonpositive=0  above_INT_MAX=0
```

**D12 정밀화**(★): "헤더 순서 자체"는 920/920 정상이나, **geometry와 대조한 완전
D12**로는 도달가능 909 중 **906 정상 · 3 실패(결함 3덱)**, geometry 부재로 선점되는
파일 11개. `data/model/abundances.csv`는 `shell_id` 스키마의 다른 형식이나
**이 판독기 경로를 타지 않는다**(★ `src/lumina_main.c:111,113,119` + 소비처 grep 0건).

### 4b. WARN — 메시지 출력 + 종료 0 (**2개 ID**)

| ID | 조건 | 근거 |
|---|---|---|
| D5 | `atom_masses.csv`에 있는데 CSV에 없는 Z | **정본이 이 상태**(누락 Z 7개). ★ 저장소 44파일 해당 |
| D6 | 셸별 `\|Σ_Z X − 1\| > 1e-6` | ★ **정상 Lumina 형식 106덱 중 43덱**이 해당. DDC15는 untracked를 inert mass로 남기는 설계 |

**D5 출력 형식**: Z마다 한 줄이 아니라 **집계 이벤트 1건 + 누락 Z 목록 1줄**.

### 4c. 문서

| ID | 내용 |
|---|---|
| D11 | `:826` 주석의 `shell0,...,shell29` 표기 정정. **게이트 아님** |

### 4d. 판독 루프 구조 정정

- **`elem_idx < n_elements`에서 멈추지 말고 EOF까지 읽는다** (★ `:825`). 그래야
  D9(중복 Z)·D13(행 0개)·초과 행이 검출된다.
- 헤더를 버리지 말고 **파싱해 D12에 쓴다** (★ `:823`).
- ⚠ **행 수 == 원소 수를 검사하지 말라.** 정본은 `abundance 8행 < atom_masses 15행`
  이며 이것이 정상이다(★ 코퍼스 `lt=44, eq=876, gt=0`, **초과행 0건**).
  등식 검사를 넣으면 정본이 죽는다. EOF 전환으로 새로 적재되는 기존 값은 없다.

### 4e. 금지

- **clamp·정규화·보정 금지.** 어긋난 입력을 고쳐 통과시키지 않는다(규약).
- **WARN을 침묵으로 구현 금지.** WARN도 stdout에 반드시 남는다.

### 4f. 음성 대조 (게이트 자격 요건)

FATAL **16개 ID** 각각 결함 주입 → **비영 종료** 시연.
WARN **2개 ID** 각각 결함 주입 → **메시지 + 종료 0** 시연.
D11은 실행 조건이 아니므로 대상 아님.

**FATAL 16개 = `D1, D2, D3, D4, D7a, D7b, D7c, D8, D9, D10, D12, D13, D14, D15, D16, D17`**
(운전석이 v2에서 9, v3에서 11로 두 번 오산했다. 구현자는 이 목록을 세어 확인할 것.)

⚠ 픽스처가 결함을 **표현할 수 있는지** 먼저 확인하라. 2026-08-03 `stage32_rung1`에서
픽스처가 경계값을 표현하지 못해 잘못된 가드가 v4까지 살아남은 전례가 있다.

---

## 5. 발주 C1·C2 — 조성 생산 경로 차단 (scripts)

### C1. 원자데이터 확장기에서 조성 생산 제거

`scripts/expand_atomic_data_cmfgen.py`:
- `write_abundances()` 호출(`:1717`) 제거
- `DEFAULT_ABUNDANCES`(`:301`)·`N_SHELLS`(`:300`) 삭제.
  다른 소비처가 있으면 **삭제 대신 사유를 남기고 보고**(조용한 존치 금지)
- docstring(`:14`)의 `abundances.csv` 산출 선언 삭제

### C2. 덱 드라이버 **5기** 수리

★ Codex 전수(`rg -l '(module|expand)\.OUT_DIR\s*=' scripts/*.py`):
```
scripts/deck_quarantine_driver.py          ← v2 누락. _ftos 조성을 필터링해 _active 에 재기록
scripts/deck_regen_fullcov_driver.py       ← 30열 결함을 그대로 승계할 수 있다
scripts/deck_regen_r1_vintage_driver.py
scripts/deck_regen_r4_ftos_driver.py
scripts/deck_regen_r4_offcontrol_driver.py
```
각각:
- `REBUILT`에서 `abundances.csv` 제거 ⟹ `copy_companions()`가 정본 복사
- 심링크 해소해 **실체 파일로 복사**
- 복사 직후 형상 검증: 열 수 == `geometry.csv` 행 수. 불일치 시 비영 종료
- `deck_quarantine_driver.py`는 필터 출력에도 같은 형상 게이트를 건다

### C2b. 생산경로 대장 기재 (수리 대상 아님, 계보 기록)

★ 별도 abundance writer 3개를 **별개 계보**로 대장에 남긴다:
```
scripts/build_toy06_epoch.py:190           ← 현 정본 생성기 (§2.6)
scripts/build_toy06_cmfgencomp_deck.py:124
scripts/build_toy06_standart_deck.py:96
```
★ `slurm_*.sh` writer는 **138개**이나 **toy06 명시 writer는 0개** ⟹ 범위 밖.
(v1의 "30여 곳"은 운전석 오추정.)

---

## 6. 발주 C3 — 결함 덱 3기 조성 복원

- `_fullcov`·`_links`·`_ftos`의 `abundances.csv`를 **현 정본에서 복사**
- 덱 전체 재생성 금지 — **조성만** 교체(단일변수 원칙)
- 기존 결함 파일은 `abundances.csv.defective_20260803`로 **보존**. 삭제 금지(규약)

**⚠ C3가 달성하는 것은 「계보 일관성」이지 「CMFGEN 동일성」이 아니다.**
§2.6에서 출처가 다름이 확정됐으므로, C3 후에도 Lumina 조성 ≠ CMFGEN 조성이다.
C3는 **오늘 밤 파괴를 6/29 상태로 되돌리는 것**이다. 대장에 그렇게 적는다.

---

## 7. 발주 E — 조성 차이 정량화 → **완료** (§2.7)

E의 핵심 질문은 **운전석 실측으로 답이 나왔다.** §2.7에 수치가 있다.

| 단계 | 상태 |
|---|---|
| E1 생성기·직접입력 특정 | ✅ 완료 (§2.6). 로그 부재로 "실행됐다"는 강한 추론 |
| E4 정본 대 CMFGEN 입력 정량 대조 | ✅ **완료** (§2.7) — 지배원소 ≤3.33%, 밀도 0.07% |
| E2 `snia_toy06_19.48d.dat` 반입 | 보류 — 대조가 `/gpfs` 직독으로 끝났으므로 반입 불요. **필요해지면 별건** |
| E3 19.48d 출처 덱 생성 | **취소** — 아래 위험 참조. 필요해지면 별건 |
| E5 붕괴식 단독 검증 | **별건으로 이월**. 아래 방법 사전등록 |

### ⚠ E3를 취소하는 이유 (★ v3 검수)

`scripts/build_toy06_epoch.py`의 `main()`은 조성구성 → 전자밀도 → **광구 탐색** →
재격자를 한 경로에 결합하며 입력·붕괴 생략 옵션이 없다. 19.48d판으로 같은 경로를
돌리면 **광구가 3,900 → 4,025 km/s로 이동해 속도 셸 자체가 달라진다**(★ `i_phot=80`,
`max_edge_diff=125.0`). 즉 "입력만 교체"가 성립하지 않는다.

**★★ 더 위험한 것**: `build_toy06_epoch.py:226`
```python
out = sys.argv[2] if len(sys.argv) > 2 else "data/tardis_reference_toy06_19p48d"
```
**기본 출력이 현 정본이다.** 출력 인자를 빠뜨리면 E가 정본을 덮어쓰고 G1의 기준이
사라진다. 이 스크립트를 어떤 목적으로 실행하든 **출력 경로를 절대경로로 명시하고,
정본 경로와 같으면 비영 종료하는 가드를 먼저 넣는다.**

**⟹ 올바른 방법은 §2.7이 쓴 것**: 정본 `geometry.csv`의 **고정된 50개 셸 중심**에
조성만 보간하는 별도 read-only 경로. 격자를 건드리지 않으므로 단일변수가 유지된다.

### E5 이월 시 방법 (★ 검수 처방)

202-zone 중심은 `100+200k`, 807-zone 중심은 `25+50k`로 **중심 교집합 0개**다.
다만 무관한 격자가 아니라 **staggered factor-4 refinement**이며 저해상도 edge가
고해상도 edge에 202/203 포함된다(누락 = 40,400).

- **붕괴식 단독 검증**은 19.48d판 **자체의 t=0 열**에 Bateman을 적용해 **같은 807점**의
  19.48d 열과 직접 비교한다. 격자 차이가 안 섞인다.
- 1h판과의 비교는 고해상도를 **zone-mass 가중 보존형**으로 201개 완전피복 저해상도
  셀에 축약하고 마지막 부분피복 셀을 별도 표기한다. 이 비교는 **붕괴식 + 해상도**
  두 효과를 함께 포함한다고 명시한다.

### 대조 규칙 (모든 조성 대조에 적용)

- **절대차가 정본.** 영값에서 상대차는 미정의. 상대차 병기 시 분모 규칙을 명시한다.
- 셸 구간 **분리 보고**: `0–43`(완전피복) / `44`(부분 5.91%) / `45–49`(무피복).
  무피복은 CMFGEN 계산격자 밖이므로 **대조 대상이 아니라 별도 표기**.
- **floor 아티팩트를 분리 집계한다** — `X_lum == 0 且 X_cmf ≤ 2e-10`인 쌍은 실차가 아니다.

---

## 8. 사전등록 게이트

역할별 분류. v1은 성격이 다른 게이트를 한 줄로 묶어 비회귀 게이트에 "결함을
FAIL시키는가"를 물었다(부적절).

### 8a. 결함 검출 — 결함 입력에서 **반드시 FAIL**

| ID | 게이트 | 판정 |
|---|---|---|
| G1 | 세 덱 `abundances.csv`가 정본과 바이트 동일 | `cmp` 종료 0 |
| G2 | 열 수 == `geometry.csv` 행 수 == 50, 전 덱 | 정수 일치 |
| G3 | 원소 집합 == `{28,27,26,20,16,14,8,6}`, O·C 전 셸 정확히 `0.0` | 집합·값 일치 |
| G4 | 전 셸 `\|Σ_Z X − 1\| ≤ 1e-12`, **double 순차합** | 위반 0건 (★ 실측 max 2.22e-16) |
| G5 | 층화 존재: 전 셸 동일값인 원소가 0개(O·C 제외) | 카운트 0 |

**G4 주의**: 전역 법칙이 아니다. ★정상 Lumina 형식 **106덱 중 43덱**이 Σ≠1이다.
**toy06 정본의 실측 성질**로만 건다. 합산 방식은 **double 순차합**으로 고정한다.

### 8b. 음성 대조 — 정상 경로에서 결함 산출물이 **안 나와야**

| ID | 게이트 | 판정 |
|---|---|---|
| G6 | `expand_atomic_data_cmfgen.py`를 빈 출력 디렉터리에 돌리면 `abundances.csv`를 **만들지 않는다** | 파일 부재 |

### 8c. 부수피해 — 결함 입력에서 PASS해도 정상 (검출용 아님)

| ID | 게이트 | 판정 |
|---|---|---|
| G7 | 수리 전후 `levels.csv`·`line_list.csv`·`cmfgen_sigma_bf.bin` SHA-256 동일 | 해시 일치 |

**G7 봉인 절차**: 사전 해시는 **어떤 파일도 수정하기 전에** 산출해 `docs/` 아래
커밋한다. 사후에 뜬 해시는 무효다.

### 8d. D 검증

| ID | 게이트 | 판정 |
|---|---|---|
| G8 | FATAL **16개 ID** 각각 주입 결함에서 비영 종료 | **16/16** |
| G9 | WARN 2개 ID 각각 주입 결함에서 메시지 + 종료 0 | 2/2 |
| G10 | D 적용 바이너리가 **정본 덱**을 정상 판독 | 종료 0 |

**G10 기대치 사전등록** (★ 실측 근거):
```
정본 덱은 D2/D3/D4/D7/D8/D9/D10/D12/D13/D14 에 걸리지 않는다
D5  : 집계 경고 이벤트 1건 + 누락 Z 목록 [12,13,21,22,23,24,25]
D6  : 걸리지 않는다 (max dev 2.22e-16 < 1e-6)
```
**경고가 D5 1건이 아니면** D5 미구현이거나 다른 조건이 오발화한 것이다.

### 8e. E 게이트

| ID | 게이트 | 판정 |
|---|---|---|
| G11 | 반입한 `snia_toy06_19.48d.dat`의 sha256이 `/gpfs` 원본과 일치 | 해시 일치 |
| G12 | E3 재격자 산출물이 셸 0–43에서 유한·비음수·Σ 유한 | 위반 0 |
| G13 | E4/E5 보고가 절대차 기준이며 무피복 셸을 분리 표기 | 형식 검사 |

---

## 9. Codex 재검수 항목 (v3, 구현 **전**)

v1·v2 반박은 §11에 반영 완료. v3에서 **새로 확인할 것만** 본다.

1. **§4a 신규 FATAL 3종(D12 헤더스키마·D13 행0개·D14 atom_masses 무결성)이
   정상 덱을 죽이지 않는가.** 저장소 전수로 재확인하라. 특히 `logs/` 아래
   811개 파일과 `data/model/abundances.csv`(다른 형식, `shell_id` 헤더)를
   D12가 어떻게 처리하는지 명시하라 — **다른 형식 파일이 이 판독기를 타는가?**
2. **§4d 루프 구조 정정(EOF까지 읽기)이 기존 동작을 바꾸는가.** 정본 덱에서
   행 수 == 원소 수인가, 초과 행이 있는가. 있으면 지금까지 무시돼 온 것이다.
3. **§7 발주 E의 E3가 실행 가능한가.** `build_toy06_epoch.py`에서 붕괴 단계와
   재격자 단계가 **분리 가능한가**(코드 구조 확인). 분리 불가면 E3의 대안을 제시하라.
4. **§7 E5의 "공통 속도격자" 대조가 잘 정의되는가.** 1h판 202 zone과 19.48d판
   807 zone의 속도격자 관계를 실측하라. 한쪽이 다른 쪽의 부분집합인가?
5. **§1 순서 변경(C3를 E 앞으로)이 정당한가.** C3가 E와 정말 독립인가.
   E의 결과가 C3의 적재물을 바꿀 경로가 남아 있는가.
6. **§6의 "C3는 계보 일관성이지 동일성이 아니다"가 정확한가.** 이 진술이
   과소·과대 주장인지 판정하라.
7. **v3가 놓친 무증상 실패 경로가 있는가.** v2 지적(헤더순서·행0개·atom_masses
   중복·EOF·NUL)은 전부 반영했다. 그 밖에?

### 검수 산출물

`docs/CODEX_AUDIT_ORDER_CD_V3.md`.
`항목 / 판정(확인·반박·미결) / 실측 근거(명령과 출력) / 발주서 수정 요구` 4칸.
**명령과 출력을 붙이지 않은 "확인"은 무효.** 반박이 하나라도 있으면 **반려**.

---

## 10. 실행 배치 (운전석 몫)

- 구현·빌드 = Codex. **실행·투척 = 운전석.**
- D 빌드는 로그인 노드 가능(규약). **실행 금지** — 게이트 실행은 grammar-debug/slurm.
- C·E는 경량 파일 조작·감사. grammar-debug 노드.
- 판정런은 이 발주의 범위가 아니다. G1–G13 통과 후 별건 발주.

---

## 11. 반려 이력과 정정 내역

### v1 반려 (Codex, `docs/CODEX_AUDIT_ORDER_CD.md`)

| # | v1 오류 | 실측 | 정정 |
|---|---|---|---|
| 1 | 권위 사슬 출발점을 1h판으로 적음 | `mk_sn_hydro.py` SRC = 19.48d판 | §2.5 개정 + floor·재규격화 폭로 + E 신설 |
| 2 | Σ=1을 FATAL로 지정 | 43덱이 Σ≠1. DDC15 inert mass 설계 | D6 → **WARN** |
| 3 | masses에 있고 CSV에 없는 Z를 FATAL | **정본이 그 상태** | D5 → **WARN**, G10에 사전등록 |
| 4 | G7에 "결함을 FAIL시키는가"를 물음 | 비회귀 게이트라 PASS가 정상 | 게이트 역할 분류 |
| 5 | fail-open 6종만 열거 | NaN/Inf/중복Z/버퍼절단 누락 | D7–D10 신설 |
| 6 | slurm writer "30여 곳" | 138개 | 수치 정정 |
| 7 | C2 대상 1개 드라이버 | 4개 | 목록 명시 |
| 8 | 판독기 열 순회를 추론으로 적음 | 최소재현 확증 | 추론 → 확증 |

### v2 반려 (Codex, `docs/CODEX_AUDIT_ORDER_CD_V2.md`)

| # | v2 오류 | 실측 | 정정 |
|---|---|---|---|
| 9 | `G8 = 9/9` | FATAL ID는 D1–D4·D7–D10 = **8개**. 9/9는 산술 불가 | FATAL 재편(11개 ID), D7을 a/b/c로 분리 |
| 10 | "110덱 중 43" | `data/model/abundances.csv`는 다른 형식(`shell_id`). 정상 Lumina 형식 = 109−3 = **106**, D6 = 43/106 | 분모 정정 |
| 11 | C2 대상 4개 | **5개** — `deck_quarantine_driver.py` 누락. `_ftos`→`_active` 필터로 **30열 결함 승계 가능** | 목록 확대 |
| 12 | E를 "편집 없음"으로 규정하고 §6에서 파일 반입을 지시 | 자기모순 | E를 **provenance 반입 포함**으로 재분류 |
| 13 | 헤더 순서 무검증 | **역순 헤더가 동일 결과로 적재됨** | **D12 신설** |
| 14 | 데이터 행 0개 무검증 | **전 FATAL 통과, 전 조성 0인데 종료 0** | **D13 신설** |
| 15 | `atom_masses.csv` 무결성 무검증 | 중복 Z가 D4/D5/D9 전부 통과 | **D14 신설** |
| 16 | 판독 루프가 `n_elements`에서 정지 | 초과 행·중복 미검출 | §4d **EOF까지 읽기** |
| 17 | E의 대조 규칙 미정 | 영값에서 상대차 미정의 | §7 **절대차 + 분모 규칙 + 셸구간 분리** |
| 18 | 정본 생성기 "미확인" | ★ `build_toy06_epoch.py`로 특정, byte_equal | §2.6 신설 — **출처 상이 확정** |

### v3 반려 (Codex, `docs/CODEX_AUDIT_ORDER_CD_V3.md`)

| # | v3 오류 | 실측 | 정정 |
|---|---|---|---|
| 19 | `G8 = 11/11` | FATAL ID는 **13개**(D7 a/b/c 분리 반영 누락). **운전석이 같은 산술을 두 번 틀림** | v4에서 D15–D17 추가 후 **16개**로 확정, 목록을 명시 열거 |
| 20 | E3를 "입력만 교체"로 규정 | `main()`이 조성→전자밀도→**광구탐색**→재격자 결합. 19.48d판이면 광구 3,900→4,025, `max_edge_diff=125` ⟹ 격자가 달라짐 | **E3 취소.** 고정 셸중심 보간 read-only 경로로 대체(§2.7이 그 방법) |
| 21 | E가 "기존 파일 변조 없음"이라 선언 | ★★ `build_toy06_epoch.py:226` **기본 출력이 현 정본**. 인자 누락 시 정본을 덮어씀 | §7에 위험 경고 + 출력경로 절대지정·정본충돌 가드 의무화 |
| 22 | E5 "공통 속도격자" | 202존 중심 `100+200k` vs 807존 `25+50k` ⟹ **중심 교집합 0**. staggered factor-4 refinement | E5를 둘로 분리(붕괴식 단독 = 807점 자체 t=0 열 / 해상도 비교 = zone-mass 보존형 축약) |
| 23 | 인식행 1개가 전부 0 | 전 FATAL 통과, D6 WARN만, 종료 0 | **D15 신설** (셸별 ΣX ≤ 0) |
| 24 | `X = 1.2` | 전 FATAL 통과 | **D16 신설** (X > 1) |
| 25 | 수치 파서 범위 무검사 | `strtod("1e-9999")`→0.0 errno=34 / `strtol("4294967302")`→int cast **6 = Z=6 위장** | **D17 신설** (ERANGE) + D14에 Z 양수·표현범위 |
| 26 | 행 수 == 원소 수 가정 가능성 | 정본은 **8 < 15**. 코퍼스 `lt=44, eq=876, gt=0` | §4d에 등식검사 금지 명시 |

**운전석 오류 성격**: 26건 중 **6건이 치명적 부류**였다 —
정상 덱을 죽이는 게이트 3건(#2 Σ=1 FATAL, #3 D5 FATAL, #26 등식검사),
계약을 안 닫는 반례 3건(#14 행0개, #23 전부0, #24 X>1).
거기에 **정본을 덮어쓸 뻔한 것 1건(#21)**과 **같은 산술 두 번 오산(#9·#19)**.
세 차례 검수가 전부 구현 전에 잡았다. 검수 관문을 발주서에 두는 규약의 값어치가 이것이다.

# 격자 포함 계약 단(안 B) 판정 — 2026-08-08 07:5x

사전등록 `docs/RUNG_GRID_CONTAINMENT_CONTRACT.md`.

## 판정: **PASS** (자기 계약에 한해).  차단 사유가 격자에서 완전히 떠났다.

| 게이트 | 판정 | 근거 |
|---|---|---|
| **B-1** 정렬 | **PASS** | 파생식이 `j=0 ≡ NLTE_NU_MIN` 이므로 **구성상** 성립. `_Static_assert` 로 K·오프셋·빈수 동결 |
| **B-2** ★왕복 항등식 | **PASS** | `max_abs=0 max_rel=0` — 기계정밀도가 아니라 **비트 단위 정확 일치** |
| **B-4** REBIN-DIAG 미발화 | **PASS** | 발화 **0회**, `missing_term_delta=0` |
| **B-5** 범위 포함 | **PASS** | 새 `[1.43909e12, 4.03626e16]` ⊃ 구 `[1.44029e12, 4.03242e16]` |
| **B-6** 값 이동 장부 | **측정 완료** | 아래 |
| **B-3** A2-10 통과 | **미달 — ★게이트를 잘못 썼다** | 아래 |

```
[GRID-ROUNDTRIP][PASS] K=2 source_bins=1000 canonical_bins=3866 anchor=1754
    canonical_range=[1439089991726.9829,40362581455823112]
    max_abs=0 max_rel=0 worst_bin=0
    NB3_narrow=PASS NB4_oog_rejected=PASS shortfall_control=PASS
```

★자가검사가 음성 대조를 **함께** 시연한다: NB3(정당하게 좁힌 소비자 격자는 **통과** —
과잉 게이트 아님) · NB4(`OUT_OF_GRID` 를 0 으로 읽지 않고 거부 — 클램프 재발 방지).

## B-6 값 이동 — 결정론 팔에서 **한 줄만 바뀌었다**

```
물리 줄 격자前=159  격자後=159
  < reason=RADEQ_TERM_MISSING
  > reason=RADEQ_NO_BRACKET
```
출력된 물리 수치는 **하나도 움직이지 않았다.**
⚠단, canonical 장의 **빈별 값은 필연적으로 바뀐다**(4000→3866).  그 값들은 이 로그에
표면화되지 않으므로 "안 움직였다"가 아니라 **"표면화된 것 중에는 없다"** 로 기재한다.
MC 팔의 이동은 전송이 죽어 있어 **사건 측도 단(E) 이후에만** 잴 수 있다.

## ★B-3 은 내가 단 경계보다 넓게 쓴 게이트다

B-3 을 "결정론 팔이 A2-10 을 **통과**하고 T_e 세대 1→2 커밋" 으로 썼다.
그것은 **격자 계약이 아니라 복사평형 계약**이다.  오늘 새벽 R7 에서 똑같은 실수를 했고
그때 원장에 이렇게 적었다 — *"판정 기준은 단 경계와 정확히 같아야 한다.
넓히면 남의 단의 실패를 내 단의 실패로 적게 된다."*  또 그랬다.

격자 단의 계약은 "포함 관계가 선언·검사된다" 이고 그것은 **충족됐다**.

## 다음 전선 — 배선이 아니라 **물리**다

```
전:  [A2-10][BLOCKED] reason=RADEQ_TERM_MISSING  missing_term_delta=1   (입력을 모을 수 없다)
후:  [A2-10][BLOCKED] reason=RADEQ_NO_BRACKET    no_bracket_delta=1     (입력을 모았고 풀었으나 근이 없다)
```

이제 A2-10 이 **실제로 솔브를 돈다**.  그리고 `[10 K, 1e7 K]` 구간 양 끝에서 잔차 부호가
같다 — 즉 그 범위 어디에서도 에너지 균형이 0 이 되지 않는다.

이것은 **배선 결함이 아니라 첫 정직한 물리 실패**다.  R7·Γ·R6·격자B 네 단이
전부 이 지점을 향해 있었고, 이제 처음으로 "가열과 냉각이 맞지 않는다" 를 볼 수 있다.

R8 보존은 이번에도 유지됐다(`te_manifest_preserved=1 generation_preserved=1`).

## 검사에서 잡은 것

- **덱 불변 확증**: `NLTE_*` 가 diff 양쪽에 나와 걸렸으나 **값이 완전히 동일한 이동**
  (파생을 위해 헤더 앞으로).  덱 `cmfgen_sigma_bf.bin` 무영향
- **clamp 오탐 2건**: `fmax(lo,edge)`/`fmin(hi,edge)` 는 구간 교집합 계산이고
  기존 `a210_rebin_checked_J` 와 같은 관용구다.  클램프가 아니다
- **Makefile 규칙 누락**: 자가검사 소스만 오고 빌드 규칙이 빠져 손으로 빌드해야 했다.
  규칙을 메우고 헤더 드리프트 검사 재실행(21/21 PASS).  다음 발주 요건에 넣었다
- 격자 정체성 SHA 갱신으로 **구 격자 자산이 의도적으로 무효화**된다 — 조용한 재사용 없음

---

# ★정정 (2026-08-08, Codex 감사 `validation/audit/CODEX_AUDIT_2026-08-08.md`)

**이 단은 "폐합" 이 아니라 "부분 폐합" 이다.**  아래 절의 판정 일부를 철회한다.

| 철회 | 사유 |
|---|---|
| **B-6 측정 완료** | B-6 은 격자 변경이 값을 **어디서 얼마나** 움직였는지 재는 항목이다.  "표면화되지 않는다" 는 **정당한 유보가 아니라 미측정**이다.  차원이 4000→3866 으로 바뀌므로 공통 BF 격자에 양쪽을 투영한 장·적분 모멘트·위치 결박 매니페스트를 비교해야 했다 |
| **"첫 정직한 물리 실패"** | **과장**이다.  현재 잔차 정의 자체가 CMFGEN 과 다르므로(BF 냉각 정의·signed χ_bf) 물리 실패라 부를 자격이 없다 |
| **"그 범위 어디에도 균형이 없다"** | ★**수학적으로 틀렸다.**  양 끝이 음수여도 중간에서 양수가 되어 **두 근**을 가질 수 있다.  증명되는 것은 "현재 이분법이 이 구간으로 bracket 을 만들 수 없다" 뿐이다 |

**남는 것**: 포함 계약 자체(B-1·B-2 왕복 항등식 `max_abs=0`·B-4·B-5)는 유효하다.

# LUMINA-SN 프로젝트 상설 규약 (세션 자동 로드)

## 실행 환경 티어 (user 2026-08-01 확정)
- **경량 오프라인**: **로그인 노드(syntax·grammar) 실행 전면 금지(user 08-01)** — 파일 조회·grep·판독 등 비연산만 로그인 허용. 단일 CPU 소형 작업은 **grammar-debug 노드**(nested ssh: `ssh grammar "ssh grammar-debug '<cmd>'"`; 32코어·/gpfs·/home 공유·슬럼 불요) 또는 slurm job-per-run. 대형은 아래 티어.
- **대형 오프라인**(분 단위 초과 — KA 전체 사다리·고정밀 oracle·대형 리플레이): **lageunha 직접 투척**(ssh+백그라운드, OMP=60). 투척 전 `ssh lageunha uptime`으로 부하 확인 — 선주 작업(예: ramses_zoom3d) 포화 시 grammar CPU slurm(양호 노드 지정)으로 폴백. 타 사용자 등장 시 즉시 양보.
- **CMFGEN**: grammar CPU slurm 또는 lageunha 수동만(syntax 금지). **OMP=16 절대.** slurm은 `--time` 명시(백필 자격).
- **GPU 생산 런**: slurm job-per-run, 파티션 h200→h100(full-NLTE 80GB — a40 제외). 상주 러너·interactive 할당 금지.
- **분업**: codex=스크립트 작성까지, 제출·투척·대외 발송=운전석(Claude). codex 샌드박스는 네트워크 차단됨.
- **불량 노드 상시 제외(user 08-01 지시, 관리자 해소 확인 시까지)**: grammar 제출 시 **모든 sbatch에 `--exclude=grammar072,grammar078,grammar080` 기본 적용**(072=/gpfs 기록 불가 의심·078/080=/home 마운트 불량). slurm 산출 경로는 /gpfs(GPFS scratch 규약).

## 검증 규약 (요지 — 상세는 memory/MEMORY.md 자동 로드)
- 물리 복원 사다리: 수리 단위=물리 계약 1개, rung당 패치+기대 변경집합 사전등록, B=증분 byte-배터리.
- 음성 대조 의무: 게이트는 주입 결함으로 FAIL을 시연해야 PASS 자격.
- clamp/floor 금지(판별식: 정확해가 위반 가능한 가드·표현은 잘못된 것). 발견의 처분=조용한 대장 기재.
- src-편집/트리-변조 태스크는 한 번에 1개(발주 전 가동 중 변조 태스크 0 확인). C 리뷰는 안정 산출물(패치 파일)만.
- 런 발주 3요건(offline-first): 기전 오프라인 특정·수리안 오프라인 검증·기대치 사전등록 후 판정런 1회.


## ★Fable 위임 상시 승인 (user 2026-08-20) — 하네스 지시와의 충돌 해소

하네스는 세션마다 **"user 가 요청하지 않으면 Agent 를 호출하지 말 것"** 을 주입한다.
그런데 분담 개정14 의 **Fable 몫 넷은 전부 Agent 호출을 요구**한다. 이 충돌을 운전석이
말없이 하네스 쪽으로 해소한 결과, 08-20 DET-PHYSCMP 단에서 **Fable 몫 4건 중 3건을
운전석이 겸했다**(사전등록·코드 검수·판정).

**user 상시 승인 (원문: "오케이 그렇게 줄께")**: 아래 **네 역할에 한해** 운전석은 매번
묻지 않고 Fable 서브에이전트에 위임한다. 이것이 하네스가 말하는 "user 의 요청"이며,
**새 세션에서도 이 파일이 로드되는 순간 유효**하다.

| 위임 대상 (이 넷만) | 비고 |
|---|---|
| ① **사전등록** (단 설계·계약·게이트·기대치) | 런/발주 **전에** 커밋 |
| ② **코드 검수** | 고정질문에 *"발주서가 사전등록의 범위를 좁혔는가"* 포함 |
| ③ **판정** (판정문 저작) | |
| ④ **판정 감리** | ★①③ 과 **다른 fresh 컨텍스트**(자기 채점 금지)·고정질문 4 |

⚠**이 넷 밖의 Agent 호출은 여전히 user 요청이 있어야 한다.** 백지 위임이 아니다.
⚠**분장 장부 의무**: 모든 단 문서는 `단계 / 규약상 담당 / **실제** / 위반` 3열 표를 갖는다.
규약상 담당만 적는 것은 금지(08-20 판정문이 실제로 그렇게 허위 기재했다).

## 발주 전 의무: 사전등록 기계 프리플라이트 (user 2026-08-21)

**모든 Codex 발주 전에** `scripts/check_prereg_preflight.py <사전등록> --root .` 을 돌린다
(grammar-debug). rc≠0 이면 **발주 금지**. 사전등록은 ```prereg-preflight 선언 블록을 갖는다 —
없으면 게이트가 거부한다(fail-closed·조용한 건너뛰기 금지). 게이트 자신의 음성대조는
`--selftest`(NC 9종, `NC-P0`=성한 계약은 통과해야 한다 포함).

**근거(2026-08-21 실사고)**: DET-SPRIM-L6 이 발주 4회에 계약 결함 5건을 냈고 판정런은 0회
소모했다. 셋은 한 번만 실행해 보면 잡히는 종류였다 — 명세가 **검증 불가능한 모드**에서
읽기만으로 감당 못 할 크기로 쓰인 것이 원인이다. 코드에는 게이트가 겹겹인데 **가장 상류인
계약에는 검사가 없었다**(「고리 밖 감사」가 지목한 그 형태).

**한계(과대 주장 금지)**: 5건 중 **3건**을 잡는다. 정적 참조가 아닌 **의미 검사**(예: 정확-건수
강제의 함의)는 못 잡는다.

### ★개정16 — 장부 단일화·검수 선행·저자 실행 의무 (user 2026-08-22)

세 건. **배분(개정14)과 Fable 넷의 분리는 불변** — 이번 캠페인에서 감리가 판정문의 10자릿수
오류를, 검수가 봉인 파괴를, 판정자가 커밋 문면을 잡았다. **셋 다 앞 단계가 못 본 것**이라
컨텍스트 분리의 값이 실측으로 났다. Codex 의 "어긋나면 멈추라" 도 유지한다(최후 방어선).

**① 분장 장부는 한 곳에만 둔다.** 정본 = **집행 기록**(사전등록 문서 안). 판정문·감리문은
장부를 **복제하지 않고 지목**한다("장부 = 사전등록 §N"), 자기 단계 칸만 기입한다.
근거: 같은 표를 세 곳에 쓰면 뒤의 둘은 **전해 들은 것을 옮겨 적는 것**이고, 옮겨 적기는
표류한다(운전석 발주서 결함 4건이 그 계급).

**② 검수가 봉인 접촉 게이트보다 앞선다.** 코드 검수 고정질문의 **첫 항목**은
*"이 산출물을 봉인·실물에 돌려도 되는가"* 다. 그 답이 오기 전 운전석은 **봉인 접촉 게이트를
돌리지 않는다.** 봉인 무접촉 게이트(빌드·selftest·배터리)는 병행 가능.
근거: DET-SPRIM-L6 에서 검수가 잡은 차단 결함(stager 가 하드링크로 봉인 base 를 침묵 파괴)이
운전석의 게이트 실행 **도중**에 나왔다 — 그 사이 실물에 돌렸으면 다시 만들 수 없는 증거가
날아갔다.

**③ 저자가 실행 가능한 것은 저자가 실행한다.** `grep`·`sed`·파일 실존 확인처럼 로그인 노드
비연산 범위에서 되는 것은 **사전등록 저자가 실행**하고 `[저자 실행]` 으로 표기한다.
`[미실행]` 은 **저자가 실행할 수 없는 것**(대용량 해시·클러스터 연산·빌드·GPU)에만 쓴다.
근거: 개정15 §3-3 이 표기를 만들었으나 실행 주체가 운전석으로 몰려 있어, 두면 사전등록이
점점 더 많은 것을 `[미실행]` 로 넘기게 된다.

### ★개정15 — 발주 형식 (트리거 발동 2026-08-22, `docs/LABOR_REDIVISION_2026-08-22.md`)

**발주 프리플라이트 의무**: 모든 Codex 발주 전 `scripts/check_dispatch_preflight.py <발주서>
<사전등록> --root .` rc=0 이어야 한다(grammar-debug). 음성대조 `--selftest` NC 8종
(`NC-D0`=성한 발주서는 통과해야 한다 포함).

★**뒷겹 재서술 전면 금지**: 발주서 뒷겹은 계약의 어떤 값도 적지 않는다 — 파일 이름·파일 수·
NC 이름·NC 개수·커밋 해시·임계값·env 값 전부 **절 번호로만 지목**한다("§4 표의 행 전부",
"§5 가 요구하는 음성대조 전부"). §0 경위 절은 면제하되 *"이 절의 값은 요구가 아니다"* 문구를
달아야 한다. **근거**: 발주 중단 9건 중 4건이 운전석 발주서였고 넷 다 뒷겹의 재서술이 표류한
것이다(파일 수·계약 밖 요구·커밋 해시·개정 후 NC 목록 미갱신).

**사전등록의 사실 주장**: 봉인·실행 상태를 주장할 때 **명령줄을 병기**하고, 저자가 실행하지
않았으면 `[미실행]` 을 표기한다. 운전석은 발주 전 `[미실행]` 주장을 **전건 실행**한다.

**개정 후 재발주**: 사전등록이 개정되면 **발주서를 처음부터 다시 쓴다**(서두만 교체 금지).

**집행 기록 필수 4항목**: 발주 프리플라이트 rc · `[미실행]` 주장 실행 결과 · 분장 장부(검수
완료 후 기입) · 발주 중단이 있었다면 사유와 주인.

### ★트리거: 발주 2회 연속 실패 → 세부 업무 재분장 (user 2026-08-21)

발주가 **계약 결함으로 중단**되는 일이 **2회 연속** 더 발생하면, 운전석은 승인을 기다리지 않고
**세부 업무분장 재작성**에 들어간다. 개정14 의 역할 배분이 이 규모의 명세를 감당하지 못한다는
증거로 취급한다.
**계수 상태(2026-08-21)**: 4차 발주 진행 중 = 트리거 카운터의 첫 칸. 4차·5차 연속 중단 시 발동.

### 사전등록 개정의 형식 (2026-08-21)

**절 통째 교체 금지.** 개정은 `OLD:`/`NEW:` 쌍으로만 받고, 삭제하는 텍스트는 반드시 `OLD` 로
명시한다. 적용 후 **행 단위 diff** 로 의도 외 소실 0 을 확인한다.
근거: 개정 1 이 §4 를 통째 교체하며 `lumina_plasma.c` 행을 소리 없이 **치환**했고
(행 수 불변이라 눈에 안 띔) 운전석이 못 잡았다 — Codex 2차가 잡았다.

## 논문 (Overleaf)
- 저장소: `~/BACKUP/Eunha.A1/Claude/overleaf_lumina_paper1` (git.overleaf.com/6a6d705cc431f91124274896). 격식 영문은 **~/WRITING.md 전 규칙** 준수 + 제출 전 자가점검 grep. 그림은 python(matplotlib)+내장 레이아웃 검사기 0위반. 푸시 전 `git pull --rebase`(웹 수정 보존).
